# HANDOFF - 2026-07-30 00:41

## 使用ツール
Claude Code（Opus 5）。iOSシミュレーター制御MCP、`xcodebuild` CLI を使用。Codex CLI は今回未使用（委譲ルールをAGENTS.mdに整備しただけ）。

## 現在のタスクと進捗

- [x] プロジェクト構成の整備（make_project モードB）：`.agent/` `.spec/` `.claude/commands/` を作成、`AGENTS.md` / `CLAUDE.md` / `GEMINI.md` を追加
- [x] `AGENTS.md` を164行まで整備：目的、作業対象の優先順位、Git運用、iOSビルドとテスト、Swift/SwiftUI規約（SwiftData+CloudKit制約含む）、TestFlight運用、触ってはいけないもの、Codex委譲ルール
- [x] `README.md` を vinext-starter のテンプレートから Subsc の説明へ書き換え
- [x] 未マージだった TestFlight ビルド2〜4 の iOS 作業（約2,000行）を main へ取り込み。main が4日以上古い状態だったのを解消
- [x] 収益化方針の決定：広告（AdMob）は採用せず、1.0.0は完全無料でリリース。収益化はリリース後に判断し、実施時は既存機能を制限せず新規機能の有料化で行う
- [x] 機能追加：カテゴリとカラーのカスタム追加、一覧行へのメモ表示とカテゴリバッジ化（`3147347`、main へマージ・push済み）
- [ ] **1.0.0 のリリース作業**：`ios/Subsc/AppStore/RELEASE_RUNBOOK.md` の未完了項目が残っている。実機TestFlight確認、App Store Connect の入力、公開URLの用意はいずれも haruto さんの作業
- [ ] `Features/Dashboard/ReportCard.swift`（776行）の分割。AGENTS.mdの400行目安を超過（TODO優先度：中）
- [ ] UIテストの実行確認。CLIからは実行できないため未検証

## 試したこと・結果

### 成功したアプローチ
- **TDDで純粋ロジックを先に切り出す**：`ColorHex`（16進数変換）と `CategoryCatalog`（選択肢生成・入力検証）をSwiftUI非依存にしたため、モデルコンテナなしでテストできた。28件追加して合計52件合格
- **カテゴリを専用モデルにせず、登録済みサブスクから導出**：CloudKitのRecord Type追加とProductionスキーマ反映を回避できた。トレードオフ（最後の1件を削除するとカテゴリも消える）はユーザー承認済み
- **`project.pbxproj` の手動編集**：ID規則（`AA`=PBXBuildFile / `BB`=PBXFileReference / `DD`=PBXGroup）に従い1ファイル4箇所を追加。`xcodebuild test` まで通して検証した。手順は `.spec/KNOWLEDGE.md` に記録
- **シミュレーターでの目視確認**：`xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large` で最大文字サイズを再現し、レイアウト破綻がないことを確認できた
- **AdMob導入の是非を、実装前に調査して影響範囲を提示**：審査向け前提（App Privacy回答・Privacy Manifest・ATT・Runbook書き換え）が連鎖することを示し、採用しない判断に至った

### 失敗したアプローチ・つまずき
- **古いビルドを起動して「実装が反映されていない」と誤認した**：DerivedData が2つあり（`Subsc-bstydhbfybaseugiibhdutronbvt` は 2026-07-27 の残骸、`Subsc-etldgtbrctixtodepszkzyqqpdgb` がCLIビルドの出力）、古い方を `launch` してしまった。**次回は `launch` 前に `.app` の更新時刻を確認する**
- **バイナリへの `grep` でビルド内容を検証しようとして失敗**：既存文字列すら0件になり判定に使えなかった。実際に起動して画面で確認するのが確実
- **`xcrun simctl ui` のオプション名を `content-size` と誤記**：正しくは `content_size`（アンダースコア）
- **シミュレーターの日本語IMEがASCII入力をかな変換する**：`text` アクションで "Dropbox" と打つと「Dろpぼx」になる。日本語の直接入力も不可（ASCIIのみ）。テストデータの名前が崩れるが挙動確認には影響しない
- **シミュレーターMCPの `attach` が最初に失敗**：「Xcode is installed but not selected」と出たが `xcode-select -p` は既に正しい値だった。haruto さんがシミュレーターを手動起動したら通った

## 次のセッションで最初にやること

1. `AGENTS.md` / `.agent/memory/MEMORY.md` / `.agent/handoff/HANDOFF.md` を読み込んで報告する（セッション開始時の必須手順）
2. **`.spec/PLAN.md` を確認する。** 現在は空。haruto さんがやりたいことを書いていれば、そこからヒアリングして `SPEC.md` を作る
3. PLAN.md が空の場合は、残タスクのどれに着手するか確認する
   - 1.0.0リリース作業の支援（ストア掲載用の概要文・キーワード案の作成は代行可能）
   - `ReportCard.swift`（776行）の分割
   - UIテストをXcodeから実行してもらい結果を反映

## 注意点・ブロッカー

- **UIテストは CLI から実行できない**（`xcodebuild test -only-testing:SubscUITests` が起動しない既知の制約）。今回追加した識別子は `add-category-button` と `category-validation-message` のみで既存識別子は変更していないため影響はない見込みだが、未検証。Xcodeから実行して確認が必要
- **`origin`（chatgpt-team.site）が25コミット遅れており、push できない**。`fatal: could not read Username` で認証情報がキーチェーンにない。sudo/認証は haruto さん側の作業。Web版は廃止予定なので追いつかせる実益は薄いと判断して放置している
- **push 先は `github`（Dan-fire2030/Subsc）が既定**。`origin` へは明示指示があった場合のみ
- **`github` に `docs/project-guidelines` ブランチが残置されている**（マージ済み・削除不要とユーザー判断）
- リリース前チェックリストを勝手に `[x]` にしないこと。実機確認が必要な項目は haruto さんの報告を待つ
- 審査向けの前提（独自ログインなし、データは端末と本人のiCloud Private DBのみ、為替APIへサブスク名や料金を送信しない、アプリ内課金なし）を壊す変更は、実装前に影響を説明する
- **ビットマップ画像の生成は必ず Codex CLI に委譲する**（モデルは `gpt-5.6-sol`。config.tomlで既定設定済みなので `--model` は不要）
- テスト時にCloudKitへミラーリングしない仕組み（`App/StorageMode.swift`）を変更する際は、テストが署名を要求し始めないか必ず確認する
