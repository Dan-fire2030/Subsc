# Subsc iOS Release Runbook

最終更新：2026-08-22（ビルド21のArchiveを作成。**まだアップロードしていない**）

この文書はSubsc固有のリリース情報と作業状態を記録します。パスワード、APIキー、証明書の秘密鍵は記録しません。

## アプリ識別情報

| 項目 | 値 |
|---|---|
| Xcodeプロジェクト | `Subsc.xcodeproj` |
| Scheme | `Subsc` |
| Bundle ID | `com.tonaria.subsc` |
| Team ID | `2ZR6Z7NP8H` |
| 対応端末 | iPhone |
| 最低対応OS | iOS 17以降 |
| ホーム画面名 | つきねこ（2026-08-06に`Subsc`から変更。**2026-08-11に商標を確認して確定**） |
| App Store掲載名 | `つきねこ - サブスク・固定費・ローン管理`（2026-08-08に決定・2026-08-11に確定／App Store Connectへは未入力） |
| サブタイトル | サブスクも固定費も返済も、ひとつに（2026-08-08に更新） |
| App Store Connect App ID | `6795086857` |
| SKU | `subsc-ios-20260727` |
| プライマリ言語 | 日本語 |
| 現在のバージョン | 1.0.0 |
| 現在のビルド | 20（2026-08-18にApple側の受領に成功。**このビルドで公開中**）。21はArchiveのみで**未アップロード** |
| 公開状況 | **App Storeで公開中**（<https://apps.apple.com/jp/app/id6795086857>） |
| デベロッパ表示名 | `haruto takamori`（**変更不可**。理由は下の「デベロッパ名」を参照） |

## デベロッパ名（2026-08-20に調査）

製品ページの「デベロッパ」に出る名前は **Apple Developer Program の登録名**で、
App Store Connect 側では変えられない。**このアカウントは個人（Individual）登録**である。

- 根拠：ASCのビジネス（契約）の法的名称が**個人名＋自宅住所**、納税フォームが **W-8BEN（個人用）**
- **個人登録では、屋号・ブランド名（DBA）を設定する項目自体が存在しない。**
  「つきねこ」のような名前を出すには **Organization 登録が必要**（法人登記 + D-U-N-S番号）。
  Appleに連絡すれば**同じアカウントをOrganizationへ変換できる**ため、App Transfer は不要
- 表記の修正（`haruto takamori` → `Haruto Takamori`、漢字表記など）は
  [Organization Information Update](https://developer.apple.com/contact/request/update-company-information/)
  から Account Holder が申請する。本人確認書類が要る
- **注意：名義を変えると全アプリの vendor name が更新され、IDFV がリセットされる。取り消せない。**
  つきねこは `identifierForVendor` を一切使っておらず（grepで0件）、分析SDK・MMPも無いため実害はない
- **2026-08-20時点の判断：何もしない。** 利用者から見えるブランドはアプリ名「つきねこ」で
  成立しており、本名が出るのは製品ページの1行だけ

## 未処理（期限あり）

- [ ] **Apple Developer Program 使用許諾契約の更新に同意する（期限 2026年10月2日）。**
      **Account Holder 本人**がサインインして同意する必要がある。
      同意しないと**既存アプリの更新も新規提出もできなくなる**。
      同意は取り消せないため、エージェントは押さない

## 管理画面

- App Store Connect：<https://appstoreconnect.apple.com/apps/6795086857>
- TestFlight：<https://appstoreconnect.apple.com/teams/c6305443-6aaf-448d-957e-46cd59359530/apps/6795086857/testflight>
- CloudKit Console：<https://icloud.developer.apple.com/dashboard/>

## CloudKit

| 項目 | 値 |
|---|---|
| Container | `iCloud.com.tonaria.subsc` |
| Database | ユーザーごとのPrivate Database |
| Productionスキーマ | 2026-08-03反映済み（返済の一時停止ぶんを追加） |
| Record Type（Production） | `CD_Subscription` / `CD_AmountEntry` / `CD_Loan` / `CD_LoanPayment` |
| Fields（Production） | `CD_Subscription` 32 / `CD_AmountEntry` 15 / `CD_Loan` 30 / `CD_LoanPayment` 20 |
| Record Type（Development） | Productionと同一 |
| Fields（Development） | Productionと同一 |

### 固定費機能で追加したスキーマ（2026-08-01にProductionへ反映済み）

`feat/cost-type-data-model` で追加した項目です。Developmentでの自動生成を確認したうえで、**2026-08-01にDeploy Schema ChangesでProductionへ反映しました**。

- 新規Record Type：`CD_AmountEntry`（変動費の月次実績）
  - `CD_clientID` / `CD_year` / `CD_month` / `CD_amount` / `CD_currencyRaw` / `CD_exchangeRate` / `CD_recordedAt` / `CD_subscription` / `CD_entityName`
- `CD_Subscription` への追加フィールド
  - `CD_costTypeRaw` / `CD_hasVariableAmount` / `CD_paymentMethodRaw` / `CD_paymentMethodNote`
  - `CD_endDate`（既存の`endDate`ぶん。**終了日を設定したレコードが一度も保存されておらず、フィールドが生成されていませんでした**。Optionalのプロパティは値が保存されるまでフィールド化されないため、代表データを保存して生成させました）

### ローン機能で追加したスキーマ（2026-08-02にProductionへ反映済み）

`feat/loan-repayment` で追加した項目です。**エージェントが代行して反映しました**（差分を提示し、`AskUserQuestion` で承認を得てからDeploy）。

- 新規Record Type：`CD_Loan`（22フィールド＋メタデータ6）
  - `CD_clientID` / `CD_name` / `CD_note` / `CD_repaymentMethodRaw` / `CD_interestTypeRaw` / `CD_originRaw`
  - `CD_annualRatePercent` / `CD_rateHistoryCSV` / `CD_slidingTiersCSV`
  - `CD_originalPrincipal` / `CD_borrowedOn` / `CD_totalInstallments`
  - `CD_startingBalance` / `CD_startingInstallments` / `CD_startedTrackingOn`
  - `CD_paymentDay` / `CD_bonusMonthsCSV` / `CD_bonusAmount` / `CD_isClosed`
  - `CD_createdAt` / `CD_updatedAt` / `CD_entityName`
- 新規Record Type：`CD_LoanPayment`（14フィールド＋メタデータ6）
  - `CD_clientID` / `CD_year` / `CD_month` / `CD_period` / `CD_dueOn`
  - `CD_scheduledAmount` / `CD_actualAmount` / `CD_principalPortion` / `CD_interestPortion`
  - `CD_balanceAfter` / `CD_statusRaw` / `CD_recordedAt` / `CD_loan` / `CD_entityName`
- インデックス：`CD_Loan` に54個、`CD_LoanPayment` に32個
- Security Role：`_world` / `_icloud` / `_creator` を変更

**Optionalなプロパティを取りこぼさないよう、代表データを2件に分けて保存しました。**
`borrowedOn` と `startedTrackingOn` は登録方式で排他になるため、1件では両方を生成できません。

- 「借りたときの条件から」の借入1件 → `CD_borrowedOn`
- 「今の残高から」の借入1件 → `CD_startedTrackingOn`
- 返済を1回記録 → `CD_actualAmount` / `CD_recordedAt`
- 予定表の全回 → `CD_dueOn`

反映後にProduction側のフィールドを1件ずつ読み、モデルの保存プロパティと一致することを確認済みです。

### 返済の一時停止で追加したスキーマ（2026-08-03にProductionへ反映済み）

`feat/loan-pause` で追加した項目です。**エージェントが代行して反映しました**（差分を提示し、`AskUserQuestion` で承認を得てからDeploy）。

- `CD_Loan` への追加フィールド（2つ。22→24フィールド＋メタデータ6）
  - `CD_isPaused`（INT(64)。`isPaused: Bool = false`）
  - `CD_pausedOn`（DATE/TIME。`pausedOn: Date?`）
- インデックス：上記2フィールドの `QUERYABLE` / `SORTABLE` で計4個
- Security Role：変更なし
- `CD_LoanPayment`：**変更なし**

**`LoanPaymentStatus.deferred`（停止中）を追加してもスキーマは増えません。**
既存の `CD_statusRaw`（STRING）に入る新しいrawValueだからです。繰り延べを
`LoanPayment.dueOn` の書き換えではなく実績側の状態として持つ設計にしたことで、
CloudKitへの追加が2フィールドで済んでいます。

`CD_pausedOn` はOptionalのため、値を保存するまでフィールドが生成されません。
シミュレーターで `HousingLoanA` を実際に停止し、Developmentでの生成を確認してから反映しました。

反映後にProduction側の24フィールドを1件ずつ読み、モデルの保存プロパティ23個＋`CD_entityName` と
過不足なく一致することを確認済みです。

Developmentで作成したテストレコードはProductionへコピーされません。TestFlight版とApp Store版はProductionを使用するため、同じBundle ID、CloudKit Container、互換スキーマ、Apple Accountを維持する限り、TestFlight中のユーザーデータは正式版へ引き継がれます。

モデルへ項目を追加した場合は、Development署名のアプリで代表データを保存し、CloudKit Consoleで差分を確認してからProductionへ追加反映します。Productionへ反映済みのRecord TypeやFieldは削除前提で設計しません。

## 署名とアップロード

- App Store用Export設定：`AppStore/ExportOptions.plist`
- App Store Connect直接送信用設定：`AppStore/UploadOptions.plist`
- `ITSAppUsesNonExemptEncryption = false`
- CloudKit Production entitlementを使用
- `get-task-allow = false`
- 1024×1024 App Storeアイコンはアルファなし

2026-07-27にバージョン1.0.0、ビルド1をApp Store Connectへアップロード済みです。Apple側のアップロード受領に成功し、ユーザーによる実機TestFlight確認まで進行しています。

2026-07-28にUI/UX改善とテスト追加を含むバージョン1.0.0、ビルド2をアップロードしました。Apple側の受領に成功しました。

2026-07-28にiOS 26のLiquid Glass対応とテスト実行時のCloudKit回避を含むバージョン1.0.0、ビルド3をアップロードしました。Apple側の受領に成功しました。iOS 26端末では画面全体がLiquid Glassで描画され、iOS 17から25の端末は従来の表示を維持します。

2026-07-28にレポートカードの操作改善を含むバージョン1.0.0、ビルド4をアップロードしました。Apple側の受領に成功し、App Store Connectで処理中です。サービス一覧を開くボタンを小型化し、ページ位置を示す点を削除し、現在の月や年から離れている間だけ復帰ボタンを表示します。

2026-08-01に固定費の記録、テーマ色の設定、レポートカードの可視化スタイルを含むバージョン1.0.0、ビルド5をアップロードしました。Apple側の受領に成功し、App Store Connectで処理中です。

このビルドに含めた変更：

- 固定費の記録（種別、変動費の月次実績、支払い方法、種別での絞り込み、月末リマインド通知）
- テーマ色の設定（ボタンとカードの色を個別に選択。白文字が読める範囲へ自動補正します）
- レポートカードの可視化スタイル4種類（帯、リング、バブル、縦バー）を設定画面で切り替え
- グラフの集計単位を期間で切り替え（年間換算は種別ごとの4色、月額換算は費目ごと）

**このビルドで未検証の項目です。TestFlightで確認します。**

- バブルのピンチズームとパン、カードのページ送りとの切り分け
- 縦バーの横スクロールと、カード本体の慣性スライドの共存
- iOS 17から25でのフォールバック表示（開発機にiOS 26のランタイムしかなく確認できていません）
- 費目0件、1件、全件同額、1件だけ極端に大きい場合のグラフ表示
- アクセシビリティ文字サイズでの4スタイル

2026-08-01に設定画面の見た目を直したバージョン1.0.0、ビルド6をアップロードしました。Apple側の受領に成功しました。**確認にはビルド6を使います。** ビルド5との違いは以下の3点だけで、機能の変更はありません。

- 設定画面とその配下の画面を、テーマのボタン色に追従させないようにしました。色を選んでいる最中に画面自身の色まで動くと、何を変えているのか分かりにくいためです。現在の値を示す色見本と、選んだ色を確かめるプレビューは追従したままです。
- 設定画面上部の「SwiftUI ネイティブ版」という文言を削除しました。
- 設定画面上部のアイコンを、ホーム画面と同じアプリアイコンに統一しました。これまでは別の絵柄を表示していました。

2026-08-01にバージョン1.0.0、ビルド7をアップロードしました。Apple側の受領に成功しました。 ビルド6との違いは次の1点だけで、ほかの機能変更はありません。

- バブル表示の拡大中に、ドラッグが指へ追従するようにしました。これまでは指を離した時点でまとめて反映されていました。原因は、カードのページ送りに使っている横スクロールビューの方がジェスチャーの優先度が高く、指を離すまでこちらのドラッグが解決されなかったことです。優先度を上げ、反応し始める距離も詰めました。

**このビルドで未検証の項目です。TestFlightで確認します。**

- バブルのパンが指へ追従するようになったか。**シミュレーターでは指の動きを再現できず、この修正が効いたかは実機でしか判断できません**
- バブルの拡大中にドラッグしたとき、月が変わってしまわないか。ジェスチャーの優先度を上げたため、副作用が出るとすればここです
- 縦バーの横スクロールと、カード本体の慣性スライドの共存
- iOS 17から25でのフォールバック表示（開発機にiOS 26のランタイムしかなく確認できていません）
- 費目0件、1件、全件同額、1件だけ極端に大きい場合のグラフ表示
- アクセシビリティ文字サイズでの4スタイル

2026-08-02にバージョン1.0.0、ビルド8をアップロードしました。Apple側の受領に成功しました。**確認にはビルド8を使います。**

ビルド7の実機確認で「バブル表示が前回から変わっていない」との報告を受け、原因を調べ直しました。**ビルド7の修正は的外れでした。**

- **拡大中のドラッグが指へ追従するようにしました（作り直し）。** ビルド7では`highPriorityGesture`へ変えましたが、これが効くのは同じSwiftUIビュー階層内のジェスチャーに対してだけで、ページ送りの横スクロールが内部で使うUIKitのパン認識器には勝てません。今回は**拡大しているあいだ横スクロール自体を止め**、競合そのものを無くしました。同じ原因で起きていた「ドラッグしたつもりが月が変わる」も併せて解消する見込みです。
- **バブルの横に残っていた余白を埋めました。** 領域へ収める最後の縮小が縦横の厳しいほうに合わせる等方縮小のため、横長の枠では横に約2割の余白が残っていました（スクリーンショットの実測で662×292pxの枠に対し円の占める範囲が518×280px）。半径は変えずに中心の間隔だけを広げる処理を加え、面積比と円の非重複を保ったまま領域を使い切るようにしました。

**このビルドで未検証の項目です。TestFlightで確認します。**

- 拡大中のドラッグが指へ追従するか。**シミュレーターの合成タッチでは認識器の競合が再現せず、修正前のビルドでも追従してしまいます。実機でしか判断できません**
- 拡大中にドラッグしたとき月が変わらないか
- 拡大をやめたあと、ページ送りが元どおり効くか。**スクロールを止める仕組みを入れたため、副作用が出るとすればここです**
- バブルが領域いっぱいに広がった見た目（ユニットテストでは縦横とも95%以上を確認済み。目視は未実施）
- 縦バーの横スクロールと、カード本体の慣性スライドの共存
- iOS 17から25でのフォールバック表示（開発機にiOS 26のランタイムしかなく確認できていません）
- 費目0件、1件、全件同額、1件だけ極端に大きい場合のグラフ表示
- アクセシビリティ文字サイズでの4スタイル

2026-08-02に**借金・ローンの返済管理**を含むバージョン1.0.0、ビルド9をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.`）。**確認にはビルド9を使います。**

このビルドに含めた変更：

- **借入・ローンの登録**（借りたときの条件から／今の残高から）。元利均等・無利息・リボ払い（残高スライド定額）、変動金利の見直し履歴、ボーナス返済に対応
- **返済予定表と返済履歴。** 各回の日付・返済額・元金・利息・残高を表示し、履歴から返済・滞納を記録できる。**記録の取り消しにも対応**（通知の押し間違いで完済予定日がずれたまま戻せないため）
- **返済日のローカル通知**と、通知のボタンからの「返済した」「滞納」の記録
- **試算3種**：繰上返済（今いくら入れると何ヶ月早まるか）、利率変更の影響、借入シミュレーター（未登録で試せる。設定画面に配置）
- **費目一覧への統合。** 費目と借入を「次の期日」で1本に混ぜ、種別で絞り込める
- **月間・年間レポートへ返済額を計上。** 滞納した月は0、繰上返済した月は実績の額。見込み扱いにはしない
- プライバシーポリシー本文を更新（借入と返済の記録を保存対象として明記）

**このビルドで未検証の項目です。TestFlightで確認します。**

- **ローン機能全般の実機動作。** シミュレーターで画面遷移と計算までは確認したが、実機では未確認
- **返済日通知と、通知の「返済した」「滞納」ボタンからの記録。** 通知は返済日当日に出るため発火させられていない
- 滞納を記録したあと予定が後ろへずれること、取り消しで元に戻ること（ロジックはユニットテスト済み、画面での動作は未確認）
- **ローンのiCloud同期。** Productionスキーマは2026-08-02に反映したが、2台での同期は未確認
- リボ払いの帯の入力と、元金が減らない設定を弾く動作（ユニットテスト済み、画面では未確認）
- iOS 17から25でのフォールバック表示（開発機にiOS 26のランタイムしかなく確認できていません）
- 前回からの積み残し：縦バーの横スクロールとカード本体の慣性スライドの共存、アクセシビリティ文字サイズでの4スタイル

2026-08-06に**黒猫モチーフの全面リデザインとアプリ名の変更**を含むバージョン1.0.0、ビルド11をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。**確認にはビルド11を使います。**

**このビルドは見た目が大きく変わります。** アプリ名が `Subsc` から `つきねこ` へ、アイコンが青いチェックから黒猫へ変わるため、**TestFlightの一覧で別アプリに見えます**（Bundle IDは `com.tonaria.subsc` のままです）。

このビルドに含めた変更：

- **アプリ名を「つきねこ」にした**（端末上の表示名。「つき」に月＝夜の猫と、毎月＝課金サイクルを掛けている）。**商標は2026-08-11に確認して確定**
- **アプリアイコンを黒猫（金の輪の中の顔）へ差し替えた**
- **全画面を黒猫の配色へ**（墨／白磁の地、金目のアクセント、カテゴリ7色）
- **ホームをマット・シームレスに。** 境界線と落ち影を全廃し、面の明度差と余白で構造を作る
- **相棒の黒猫がダッシュボードに常駐**し、支出の状況で6つの姿を出し分ける（過去の月では平常の姿）
- **月の進捗線**と、**これから出ていく費目の時間軸**を追加
- **グラフ4スタイルをマットへ。** 光沢・リムライト・落ち影・白飛びを全廃した
- **レポートカードの枠が切れていた不具合を修正**（ページャの高さが中身より小さく、角丸が欠けていた）
- **更新予告の通知に請求額を入れた**（変動費は次回額が未確定なので金額を書かない）
- **借入の試算機能を削除**（繰上返済・利率変更・借入シミュレーター）
- **設定から「カードの色」を削除**（ボタンの色とグラフの表示は残す）

**SwiftDataの保存プロパティは変えていません。CloudKitスキーマの反映は不要です。**

**このビルドで未検証の項目です。TestFlightで確認します。**

- **実機での見え方全般**（シミュレーターでのみ確認）
- **iOS 17〜25のフォールバック表示**（Liquid Glassが効かない系統）
- **空状態（費目0件）の案内の猫**、返済予定表・返済履歴、検索
- **金額入りの通知が実際に届いたときの文面**
- **アイコンの29ptでの見え方**と、明るい壁紙／暗い壁紙の上での馴染み

**ビルド11に混入していた不備です（2026-08-08に発見。次のビルドで直します）。**

- **画面の中に旧アプリ名「Subsc」が3箇所残っていた。** 端末上の表示名（`CFBundleDisplayName`）だけを「つきねこ」へ変え、ホーム画面のナビゲーションタイトル・設定画面のヘッダー・起動失敗時の案内文を見落としていた。**2026-08-08に修正済み（未アップロード）**
- **設定画面のアイコン素材（`AppIconPreview`）がリデザイン前の旧アイコンのまま。** ホーム画面の `AppIcon` だけを差し替えていた。**未修正**（ビットマップ生成のためCodexへ委譲する）

**独立レビューは2026-08-08にメインスレッドが実施しました**（Codexが使用上限だったため、harutoさんの指示による）。保存プロパティ・CloudKitスキーマの変更が無いこと、削除した機能の残骸が無いこと、`project.pbxproj` が壊れていないこと（`plutil -lint`）を確認済みです。指摘事項は `.spec/TODO.md` の「レビューで挙がった宿題」にあります。

### ビルド21（Archive 2026-08-22・**未アップロード**）

2026-08-22にバージョン1.0.0、ビルド21のArchiveを作成しました。
**App Store Connectへは送っていません**（harutoさんの指示でArchiveまでで停止）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-22/Subsc-build21.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 21` / `com.tonaria.subsc`
- ブランチ：`main`（`8d6dbfd` の修正 + `6a0971a` のビルド番号更新）
- **CloudKitのスキーマ変更はありません**（ビルド20以降 `Subsc/Models` に変更なし）

#### 含まれる変更

1. **空状態のボタンの文言を中央へ寄せた。** `Label` の「＋」が `.glassProminent` では
   ボタンの地の色とほぼ同じ色で描かれ、**見えないまま場所だけ取っていた**。
   アイコンと文字の組が中央に揃うため、文字だけが約22pt右へ寄っていた。文字だけにした
2. **費目別料金（グラフの内訳）の行から編集を開けるようにした。**
   引き当ては `ReportEntryTarget` に切り出してテストした。**種別（`CostType`）では
   見分けられない**（費目にも「借入・ローン」の種別がある）ため、行のIDの接頭辞で見分ける

ユニットテストは564件すべて通過。**シミュレーターで両方とも実際に触って確認済み**です
（費目・借入の両方の行から編集が開くこと、保存後に内訳がその場で更新されること、
ボタンの文字がボタン中心と2px以内で揃うこと）。

このビルドで未検証のまま残るもの：

- UIテスト（`SubscUITests`）— このMacのCLIから実行できない
- iOS 17〜25のフォールバック（iOS 26以外の端末が必要。ビルド19から継続）
- アプリを完全終了した状態での、返済日通知の「返済した」ボタン

#### アップロードする前に必要なこと

- [ ] **Apple Developer Program 使用許諾契約への同意**（期限 2026年10月2日）。
      **Account Holder本人**が同意しないと、既存アプリの更新も新規提出もできない
- [ ] 利用者へ届けるには、**1.0.1 などの新しいバージョンを作って審査へ出す**必要がある。
      ビルドを上げるだけでは公開中の1.0は変わらない

### ビルド20（2026-08-18）

2026-08-18にバージョン1.0.0、ビルド20をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-18/Subsc-build20.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 20` / `com.tonaria.subsc`
- ブランチ：`main`（`5bc9fe3` の修正 + `7faf6f4` のビルド番号更新）
- **CloudKitのスキーマ変更はありません**（ビルド19以降 `Subsc/Models` に変更なし）

#### 直したこと：通知を許可しないと費目を保存できなかった

**ビルド19が審査で却下されたための修正です**（2026-08-18 / Guideline 2.1(a) Performance -
App Completeness / 審査端末 iPad Air 11-inch (M3) / iPadOS 26.6）。
指摘は「we were unable to save item unless push notification is active」で、
添付スクリーンショットには「費目を追加」画面末尾の赤いエラーがそのまま写っていました。

原因は `SubscriptionFormView+Save.swift` が、通知トグルがオン（新規追加の既定）のまま
許可されなかった場合に**保存せずに戻していた**こと。通知を断った利用者は費目を1件も
登録できませんでした。**ローンの保存は許可の結果に関わらず保存を続けており、
サブスク側だけが取り残されていました。**

- 保存は通知の許可に関わらず常に通す
- 届かないことは通知セクションの説明で伝える。文言の判断は
  `SubscriptionNotificationFooter` へ切り出し、優先順位（許可なし＞タイミング未選択＞
  時刻超過）をテストで固定した
- 通知許可の状態は、フォームが前面に戻るたびに読み直す

ユニットテストは557件すべて通過しています。

**harutoさんが動作確認済みです（2026-08-18）。** 通知ダイアログで「許可しない」を選んだ状態で
費目を保存できること、通知セクションに許可されていない旨が出ることを確認しました。

このビルドで未検証のまま残るもの：

- iOS 17〜25のフォールバック（iOS 26以外の端末が必要。ビルド19から継続）
- アプリを完全終了した状態での、返済日通知の「返済した」ボタン

App Store Connect側の作業：

1. [x] **バージョン1.0のビルドをビルド20へ差し替えた**（2026-08-18 22:2x）。
   再読み込み後もビルド20で、保存ボタンが無効（＝未保存の変更なし）であることを確認済み
2. [x] **解決センターでAppleへ返信した**（2026-08-18 22:17）。原因・修正内容・確認方法と、
   審査にはビルド20を使ってほしい旨を英語で送信
3. [x] **「審査内容を更新」→「App Reviewに再提出」**（2026-08-18 22:3x）。
   状態は **「審査待ち」**（iOSアプリ1.0 / 1.0.0 (20)）。「未解決の問題」の表示は消えた
   - **この一押しは、harutoさんの明示的な指示でエージェントが実行した。**
     AGENTS.mdの「『審査へ提出』ボタンは押さない」に対する、この回かぎりの上書き
   - 手順の実際：バージョンページの **「審査内容を更新」を押すと、いったん
     「提出準備中」へ戻り、もう一度押すと提出物へ反映されて**、提出詳細ページの
     「App Reviewに再提出」が押せるようになる。**1回目の押下では有効にならない**

掲載文言・スクリーンショット・価格・配信地域・App Privacyは**一切触っていません**。
提出時の点検で確認した17項目は、ビルド以外そのままです。

### ビルド19（2026-08-11）

2026-08-11にバージョン1.0.0、ビルド19をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-11/Subsc-build19.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 19` / `com.tonaria.subsc` / 表示名「つきねこ」
- ブランチ：`main`
- **ビルド18の見た目の不具合2件を直しただけ**です。機能の追加はありません。
  **CloudKitのスキーマ変更もありません**（ビルド18で反映済み）

#### 直したこと：一括削除の確認が画面の外へはみ出していた

「アーカイブをすべて削除」の確認が**下向きに出て、ボタンが画面外へ出て押せませんでした**。

原因は吹き出しの向きが `.top` 固定だったこと。**`.top` は「矢印を上辺に付ける」＝元の下に出す**指定です。
このボタンは一覧の最後にあるため、下へ出す余地がありませんでした。

`deleteConfirmation` に `arrowEdge` を足し、**このボタンだけ `.bottom`（上に出す）**にしています。
**他の削除確認は `.top` のまま**です。一覧の行や詳細画面のボタンは画面の中ほどにあり、下でも見切れません。

#### 直したこと：種別と並び替えを別のボタンに分けた

1つのメニューに同居させていたため、**ボタンの見た目からいまどちらが効いているのか読めませんでした**。

- **種別**：絞り込んでいるときは種別名を出し、**ボタン自体も金色に変わります**
  （絞り込みに気づかず「無くなった」と思われるのを避けるため）
- **並び替え**：`↑` `↓` の矢印で向きが見た目に分かります

ユニットテストは549件すべて通過、Releaseビルドの警告はゼロです。
**シミュレーターで両方の直りを目視確認済み**です。

**実機で確認済みです（2026-08-12・harutoさんの報告。全項目で問題なし）。**
ビルド18の項目もすべて含みます。

1. [x] **「アーカイブをすべて削除」の確認で、キャンセルと削除の両方が押せること**
2. [x] 種別と並び替えが別のボタンになっていること。種別で絞ると**ボタンの色が変わる**こと
3. [x] ビルド18の確認項目（アーカイブの動作、残り日数、**他の端末との同期**、通知が届かないこと）

**`CD_archivedAt` をProductionへ反映した後の端末間同期は、ここで初めて実機で裏が取れました。**

### ビルド18（2026-08-11）

2026-08-11にバージョン1.0.0、ビルド18をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-11/Subsc-build18.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 18` / `com.tonaria.subsc` / 表示名「つきねこ」
- ブランチ：`main`（マージ済み）

#### 【重要】CloudKitのProductionスキーマを反映しました（2026-08-11・エージェントが代行）

**このビルドは `@Model` にフィールドを足しています。反映済みなので同期は壊れません。**

| Record Type | 追加したフィールド | 型 | 索引 |
|---|---|---|---|
| `CD_Subscription` | `CD_archivedAt` | DATE/TIME | Queryable, Sortable |
| `CD_Loan` | `CD_archivedAt` | DATE/TIME | Queryable, Sortable |

反映の手順（次に同じことをするときの型）：

1. **シミュレーターにharutoさんがiCloudでサインイン**（エージェントは認証情報を入力しない）
2. エージェントがアプリを操作し、**費目と借入を1件ずつアーカイブして保存**。
   **Optionalは値を保存するまでCloudKit側にフィールドが作られない**ため、この手順が要る
3. **モデルの保存プロパティとConsoleのフィールドを1件ずつ突き合わせ**
   （`CD_Subscription` 26件・`CD_Loan` 24件がいずれも過不足なく一致。`CD_entityName` は管理用のため除外）
4. Production との差が `CD_archivedAt` 2件だけであることを確認し、**差分を提示して承認を得てからDeploy**
5. **Deploy後にProduction側を読み直して、実際に入ったことを確認**

**エージェントは実機を操作できない。** シミュレーター用のツールは実機を映せず触れないため、
CloudKitのスキーマ作成は**シミュレーターにサインインしてもらう**のが最短だった。

#### 足したこと：アーカイブ

費目と借入を**アーカイブ（30日で自動削除される退避先）**へ移せるようにしました。削除は残しています。

- 入口は**一覧の右スワイプ**と**詳細画面**の2つ。**アーカイブに確認は出さず**、
  代わりに「復元」ボタン付きの知らせをその場に出します（30日は戻せるため）
- アーカイブ中は**一覧・レポート・カレンダー・検索候補・通知のすべてから外れます**。
  **過去の月のレポートからも外れます**（停止中と違い、ゴミ箱として一貫させる判断）
- 絞り込みに「アーカイブ」を追加。行ごとに**残り日数**を出し、**残り7日以下は色を変えます**
- 行から**復元**と**個別削除**（吹き出しの確認あり）、下に**「すべて削除」**（件数付きの確認）
- **30日ちょうどは残し、31日目で消えます。** 判定は**日付の境目**で行います
- **自動削除はアプリを開いたときにしか走りません。** バックグラウンドで消す仕組みは持ちません
- **借入をアーカイブしても返済予定表は消しません**（消すと復元できなくなるため）

**アーカイブの状態は日時1つ（`archivedAt`）で持ちます。** 状態を別に持つと日時と食い違いえますが、
日時1つなら「アーカイブ中か」と「あと何日か」の両方が決まります（`nil` がアーカイブしていない状態）。

**通知は `NotificationService.reconcile` の入口で一度だけ外しました。** 3つの計画
（リマインド・返済・更新日）はどれも `archivedAt` を見ておらず、個別に外すと1つ忘れたときに
気づけません。**消したはずのものから通知が来る**のを防いでいます。

#### 直したこと：絞り込みをチップ列にした

「アーカイブ」を足して**5段になった時点でセグメントの幅が足りず「アーカ…」と省略**されました。
横に流れるチップの列へ変え、段が増えても壊れないようにしています。

**横スクロールの合図（右端のフェード）は、実際にはみ出しているときだけ出します。**
既定の文字サイズでは5つが幅に収まってスクロールしないため、常に出すと
「先がある」と嘘をつくことになります。文字を大きくしたときに効きます。

種別の絞り込みと並び替えは**ツールバーから一覧のすぐ上・右寄せへ移しました**。
絵だけでは何のボタンか分からないので、効いている条件を短く添えています。

ユニットテストは549件すべて通過（既存527＋新規22）、Releaseビルドの警告はゼロです。

**このビルドで確認したい項目です。**

1. **アーカイブして、一覧・レポート・カレンダーから消えること**。復元で元の状態へ戻ること
2. **アーカイブ欄の残り日数**と、**「すべて削除」の確認**
3. **削除の吹き出しがシートに化けていないか**（ビルド17からの持ち越し確認）
4. **チップ列**が読めるか。**文字を大きくしたときに横スクロールできるか**
5. **他の端末との同期**。アーカイブした状態が別端末でも同じに見えるか
   （**CloudKitのスキーマを足した直後なので、ここは特に見ておきたい**）
6. アーカイブ中の費目に**通知が届かないこと**

### ビルド17（2026-08-11）

2026-08-11にバージョン1.0.0、ビルド17をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-11/Subsc-build17.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 17` / `com.tonaria.subsc` / 表示名「つきねこ」
- ブランチ：`feat/list-and-calendar-usability`（**mainへは未マージ**。実機確認が済んでからマージする）
- **`@Model` の4クラスは main から差分ゼロ。CloudKitのProduction反映は不要**

**ビルド16を実際に触ってharutoさんが挙げた5件を直したビルドです。** 機能の追加ではなく、
既にあるものの使い勝手を直しています。

#### 1. カレンダーの年月をホイールで選べるようにした

見出し「2026年8月」を押すと年と月の2列ホイールが出ます。矢印ボタンと横スワイプは残しています
（隣の月へ動くだけならそちらが速く、ホイールは遠くへ飛ぶための手段のため）。

**年の範囲は登録されている期日に追従させています。** 2000〜2100のような固定枠にすると、
中身の無い年をほとんど回すことになり、選ぶ手段としての速さが失われます。
今年は必ず含めます（過去の登録しか無いときに今年が抜けると、「今日へ戻る」で行ける月を選べません）。

#### 2. 費目一覧を支払い周期で分けた

「返済」「月払い」「年払い」のセクションに分け、見出しに件数と合計を出します。
**借入は独立したセクションにして先頭へ置きました。** 借入には支払い周期の概念が無く、
月払い・年払いのどちらに入れても嘘になるためです。検索中は分類しません。

**見出しの合計は、その下に並ぶ行の金額の合計です。** レポートの集計を持ってくる案を検討して
やめました。レポートは期間ごとの集計で停止中・履歴を独自の規則で除くため、
**停止中に絞り込んだときなどに見出しがすぐ下の行と食い違います**。

#### 3. 一覧の並び替えを足した

期日順・金額順・名前順と、それぞれの昇降。**既定は金額の大きい順**です。
入口を増やさないよう、左上の絞り込みメニューへ入れています。選んだ並びは覚えます。

**どの並びでも決着が付かないときは名前で決めています。** 順序が決まらない組が残ると、
再描画のたびに行が入れ替わります（カレンダーで同じ問題を踏んでいます）。
名前の決着は向きを反転しても昇順のままです。

#### 4. 削除の確認を、押したボタンの近くへ出すようにした

`confirmationDialog` は押した場所と関係のない位置に出るため、
**どのボタンに対する確認なのかが画面から読み取れませんでした**。
押したものを起点にした吹き出し（popover）へ変えています。一覧のスワイプ削除は、その行が起点です。

**`presentationCompactAdaptation(.popover)` は修飾子1つに閉じ込めました。**
これが無いとiPhoneでは吹き出しがシートへ化けます。呼び出し側ごとに書くと必ずどこかで忘れます。

対象は3箇所（一覧のスワイプ削除・費目の詳細・借入の詳細）。
**`LoanPaymentEditorView` の「記録を取り消す」にはそもそも確認がありません**（押すと即実行）。
位置の問題ではないため触っていません（確認を足さない判断・2026-08-11）。

#### 5. 内訳シートの配色をアプリのパレットへ揃えた

**背景に青・紫・青緑が直に書かれており、黒猫のリデザインでここだけ取り残されていました。**
段の作り（3色の斜めグラデーション）はそのままに、色の出どころだけを揃えています。

各行の色見本も、グラフ本体と同じ `BlackCatPalette.harmonized` を通すようにしました。
**2026-08-11まで内訳シートだけが保存値の色で描いており、同じ費目がグラフと内訳で違う色**でした。

ユニットテストは527件すべて通過（既存503＋新規24）、Releaseビルドの警告はゼロです。

**このビルドで確認したい項目です。**

1. **削除の吹き出しがシートに化けていないか** ← **最重要**。ユニットテストでは縛れません。
   一覧のスワイプ削除・費目の詳細・借入の詳細の3箇所
2. セクションの見出しの件数と合計が、その下に並ぶ行と合っているか。
   **停止中や履歴に絞り込んだときも**合っているか
3. 並び替えが3種類とも効き、**アプリを再起動しても選んだ並びが残るか**
4. カレンダーのホイールと、**31日を選んだ状態で2月へ飛んだときの丸め**。
   矢印と横スワイプが従来どおり動くか
5. 内訳シートの背景から青紫が消えているか。行の色見本がグラフ本体と一致するか
6. **停止中の借入がある状態での起動時のCPUと発熱** ← ビルド12以来の持ち越し・**最優先**
7. ダークモードと最大文字サイズ（**セクション見出しの「件数・合計」が潰れないか**）

### ビルド16（2026-08-11）

2026-08-11にバージョン1.0.0、ビルド16をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-11/Subsc-build16.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 16` / `com.tonaria.subsc` / 表示名「つきねこ」
- ブランチ：`main`（カレンダー画面をmainへfast-forwardマージ済み）
- **`@Model` はビルド15以降まったく変更していません。CloudKitのProduction反映は不要です**

#### 足したこと：カレンダー画面

ホームと設定のあいだに**カレンダー**のタブを追加しました。日ごとに費目の更新日と借入の返済日が並び、日を選ぶとその日の内訳が出ます。

- 週の始まりは端末の設定に従います。月送りはボタンと横方向のドラッグで行います
- **「その月に計上するか」の判断を自前で持たせていません。** `ReportCalculator.includes` を通すため、レポートの月合計とカレンダーの日合計の総和が必ず一致します（この不変条件はテストで縛っています）
- 金額と件数を出すかは設定で切り替えられます（`CalendarDisplayStore`。`UserDefaults` 保存で、CloudKitへは同期しません）
- **月送りに `ScrollView(.horizontal)` + `LazyHStack` + `.scrollTargetBehavior(.paging)` の組み合わせは使っていません**（ビルド12でCPU100%に張り付く原因になった構成のため）

#### 直したこと：通知の警告が未許可を「上限超過」と誤報していた

通知の欠落を数えて知らせる仕組みが、**通知が許可されていない環境で全件を「欠けている」と数え**、「54件の通知を予約できませんでした。上限は64件です」という自己矛盾した警告を出していました。上限64に対して54なら上限超過ではありません。`authorizationStatus` を見て `authorized` / `provisional` 以外では数えないようにしました。

併せて、知らせの**見出しと本文がずれていた**のも直しました。`operationError: String?` を3種類の知らせで使い回し、見出しは「データを同期できませんでした」に固定だったため、**通知の話が同期の失敗として表示されていました**。見出しと本文を組（`OperationAlert`）で持たせています。

#### 直したこと：裏付けの無い `remote-notification` の宣言を外した

`Info.plist` が `UIBackgroundModes: remote-notification` を宣言していましたが、`Subsc.entitlements` に **`aps-environment` がありません**。プッシュを受け取る資格が無いため、**この宣言は一度も働いていませんでした**。使っていない宣言は審査で指摘されうるので削除しました。

- CloudKit同期は従来どおり起動・前面復帰の時点で走ります。**元から届いていなかったので退行しません**
- Archiveの成果物に `UIBackgroundModes` が含まれないことを確認済みです
- プッシュで同期を受け取りたくなった場合は、App IDへPush Notificationsを足すところからやり直します

ユニットテストは503件すべて通過、Releaseビルドの警告はゼロです（`AppIntents.framework` 依存なしの通知1件のみ）。

**このビルドで確認したい項目です。**

1. **停止中の借入がある状態での起動時のCPUと発熱** ← **最優先**。ビルド12で直した無限ループそのもので、**実機では一度も確認できていません**。シミュレーターでは冷えた起動を計18試行して再現しません
2. **カレンダー画面**：ライト／ダーク両方の見え方、**最大文字サイズでマスが潰れないか**、横方向のドラッグで月が送れるか
3. **iOS 17〜25のフォールバック表示**（ずっと未検証。iOS 26以外の端末が要ります）
4. アプリを**完全に終了した状態**での、返済日通知の「返済した」ボタン（ビルド15からの持ち越し）
5. アイコンの実寸表示（29pt、明るい／暗い壁紙）、停止からの再開（`resume`）の画面動作

### ビルド15（2026-08-09）

2026-08-09にバージョン1.0.0、ビルド15をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-09/Subsc 15.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 15` / `com.tonaria.subsc` / 表示名「つきねこ」
- ブランチ：`feat/search-scroll-to-item`（**mainへは未マージ**）

#### 直したこと：通知をタップするとクラッシュする

**ビルド14で通知が届くようになった当日に発覚しました。** `.ips` をArchiveのdSYMで記号化して原因を確定しています（推測では直していません）。

```
Subsc  @objc closure #1 in LoanNotificationResponder.userNotificationCenter(_:didReceive:)
  → -[UIApplication _updateSnapshotAndStateRestorationWithAction:windowScene:]
  → -[UIApplication _performBlockAfterCATransactionCommitSynchronizes:]
  → NSAssertionHandler → abort（SIGABRT）
落ちたスレッド：com.apple.root.user-initiated-qos.cooperative（メインではない）
```

`UNUserNotificationCenterDelegate` を **`async` 版**で実装していたため、UIKitへ返す完了処理が**関数の再開先＝協調スレッドプールから呼ばれて**いました。UIKitはそこでスナップショットと状態復元を行うため、メイン以外だとアサーションで落ちます。

**素直な直し方は2つとも塞がっています。**

- `await MainActor.run { }` を挟んでも効きません。関数自身の再開先が変わらないためです
- `@MainActor` を付けた `async` 版はコンパイルできません。`UNNotification` などがSendableでなくMainActorへ渡せないためです

**完了ハンドラ版（`withCompletionHandler:`）を実装し、`Task { @MainActor in }` から自分で呼ぶ**形にしました。

併せて、**デリゲートの割り当てを `.task` から `init` へ移しました**。`.task` はビューが現れた後、つまり起動が終わった後に走ります。Appleは「起動が終わるまでに割り当てること」を求めており、遅れると**アプリが起動していない状態で通知のボタンを押した応答を取りこぼします**。

ユニットテストは486件すべて通過、Releaseビルドの警告はゼロです。

**このビルドで確認したい項目です。**

1. ~~通知をタップして、クラッシュせずにアプリが開くか~~ — **確認済み**
   （harutoさん・2026-08-09「クラッシュしなくなりました」）
2. **アプリを完全に終了した状態で、返済日通知の「返済した」を押して記録されるか**（デリゲートの割り当てを早めた箇所）
3. ビルド13〜14ぶんの未検証項目（停止中の借入がある状態での起動時のCPUと発熱、検索、
   ダークでの猫、チュートリアル、アイコンの実寸、iOS 17〜25のフォールバック、停止からの再開）

#### 通知の配信はビルド14で直っていることを確認済み

実機で通知が届き、「N件の通知を予約できませんでした」の警告も出ませんでした（＝予約が64件の枠に収まっている）。

### ビルド14（2026-08-09）

2026-08-09にバージョン1.0.0、ビルド14をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-09/Subsc 14.xcarchive`
- 確認済み：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 14` / `com.tonaria.subsc` / 表示名「つきねこ」
- ブランチ：`feat/search-scroll-to-item`（**mainへは未マージ**）

**このビルドは通知の不具合の修正だけを含みます。** ビルド13の内容はすべて含みます。

#### 直したこと：登録した費目の通知が届かない

harutoさんの報告（8/10更新の費目を前日13:30の通知で登録したが届かない。許可はオン・即時配信・登録は13:30より前・通知センターにも残らない）に対する修正です。

**原因はローカル通知の64件上限です。** iOSが1つのアプリに保持できる予約は64件で、**超過分は例外にならず黙って捨てられます**（`add` は成功を返します）。費目1件につき12回先まで予約していたため、費目11件で候補が204件ありました。

枠そのものではなく、**枠を回避する2つの経路**が壊していました。

- `reconcile` が**追加してから削除**していた。順序が逆だと、その瞬間だけ古い予約と新しい予約を同時に抱え、**上限の2.2倍（最大140件）**を要求します。**先に消してから足す**形へ入れ替えました
- 保存時の `reschedule(for:)` が**その費目だけ**を見て、全体の枠を無視して積み増していました。廃止し、`RootView` の `reconcile` へ一本化しました（保存・記録・停止のいずれも `updatedAt` か `stateRaw` を変えるため、再同期は必ず走ります）

併せて直したこと：

- 先読みする更新サイクルを **12から3へ**。開くたびに組み直すので、遠い将来の予約は他の費目の枠を奪うだけでした
- **予約後に読み戻して欠落を数え、件数を画面で伝える**ようにしました。上限超過は沈黙で起きるため、数えないと二度と気づけません
- 過ぎた時刻を黙って捨てるのをやめ、**直近の1回が届かないこと**をフォームの説明文で事前に知らせます

ユニットテストは486件すべて通過、Releaseビルドの警告はゼロです。

**この修正は実機で再現して確かめたものではありません。** 端末の予約一覧を見る手段がなく、根拠は「コードの数字（候補204件に対し枠64件）」と「報告された事実すべてと矛盾しないこと」です。

**このビルドで確認したい項目です。**

1. **費目を新規登録し、数分後に届く設定にして実際に届くか**（更新日を明日、通知時刻を今から5分後、前日）
2. **既存の費目の通知も引き続き届くか**
3. **「N件の通知を予約できませんでした」の警告が出るかどうか。**
   出れば上限超過が実在した証拠、出なければ別の要因だったことになります
4. ビルド13ぶんの未検証項目（停止中の借入がある状態での起動時のCPUと発熱、検索、
   ダークでの猫、チュートリアル、アイコンの実寸、iOS 17〜25のフォールバック、停止からの再開）

### ビルド13（2026-08-09）

2026-08-09にバージョン1.0.0、ビルド13をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-09/Subsc 13.xcarchive`
- Archive内容の確認済み事項：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 13` /
  `com.tonaria.subsc`、端末上の表示名が「つきねこ」、`Assets.car` に `AppIcon` と
  `AppIconPreview` が両方入っていること
- ブランチ：`feat/search-scroll-to-item`（`feat/onboarding-tutorial` から分岐。**mainへは未マージ**）

**ビルド12の内容をすべて含みます。** ビルド12は実機未確認のままなので、
下記の未検証項目にはビルド12ぶんも残してあります。

このビルドで追加した変更：

- **検索して見つけた項目まで到達できるようにした。**
  併せて、**検索候補を選んでも絞り込みが効かない不具合**を直した。
  `query` を入れた直後に `dismissSearch()` を呼んでいたが、`dismissSearch` は
  「検索フィールドの文字を消す」と規定されており、選んだ瞬間に検索が取り消されていた。
  Appleが用意している `searchCompletion` へ差し替えた。
  検索中はレポート・支払い予告・これから出ていく・状態のピッカーを畳み、結果を検索欄の直下に出す。
  検索中は状態の絞り込みを外す（候補は元から状態を見ないため、一覧だけ絞ると選べなかった）
- **ダークモードで猫が地に沈んで見えないのを直した。**
  体と地のコントラストが 1.04:1 しかなかった。体は墨のまま、同じシルエットをぼかして
  背後へ敷いて浮かせる。ライトは 17:1 あるため描画ごと省く。
  コントラストの回帰テストを4件追加
- **チュートリアルに猫の状態の読み方を説明するページを追加**（レポートの次。全5ページになった）
- **画面に残っていた4箇所目の旧アプリ名「Subsc」を修正。**
  通知許可の設定アプリを開けなかったときの案内が「ホーム画面の「設定」からSubscを選んで
  ください」のままだった。**ビルド12として出荷済み**。
  再発を止めるため、`Info.plist` の `CFBundleDisplayName` を唯一の出どころとする
  `AppInfo.displayName` を通す形にし、残る3箇所も同じ経路へ寄せた
- `RootView` の `try?` の戻り値が未使用という警告を解消（**Releaseビルドの警告がゼロになった**）

**SwiftDataの保存プロパティは変えていません。CloudKitスキーマの反映は不要です**（ビルド12以降、`@Model` クラスの保存プロパティに増減が無いことを確認済み）。

ユニットテストは478件すべて通過しています。

**このビルドで未検証の項目です。TestFlightで確認します。**

- **停止中の借入がある状態での起動時のCPUと発熱**（ビルド12からの繰り越し。
  シミュレーターでは冷えた起動を計18試行して再現しないが、**実機では未確認**）
- **検索**：候補をタップして1件に絞られるか／結果が検索欄の直下に出るか／
  「利用中」のまま停止中の費目を検索して出てくるか／検索を消して元に戻るか
- **ダークでの猫の見え方**（ホームの72ptとチュートリアルの大きい表示の両方）
- **チュートリアル**：5ページに増えたこと、猫の状態ページが最大文字サイズで読めるか
- **通知許可を拒否した状態で設定を開けなかったときの案内文**（旧名を直した箇所）
- **アイコンの実寸表示**（29pt、明るい／暗い壁紙）
- **iOS 17〜25のフォールバック表示**（ずっと未検証）
- **停止からの再開（`resume`）の画面動作**

### ビルド12（Archive 2026-08-08 / アップロード 2026-08-09）

2026-08-08にバージョン1.0.0、ビルド12のArchiveを作成しました。当日のアップロードはAppleの定期メンテナンスにより失敗しましたが、**メンテナンス明けの2026-08-09 08:39（日本時間）に再送し、Apple側の受領に成功しました**（`Upload succeeded.` / `** EXPORT SUCCEEDED **`）。**Archiveの作り直しは不要でした。** 失敗時の切り分けは「既知のリリース時トラブル」の該当項に記録してあります。

- Archiveの場所：`~/Library/Developer/Xcode/Archives/2026-08-08/Subsc 2026-08-08 22.10.xcarchive`
- Archive内容の確認済み事項：`CFBundleShortVersionString = 1.0.0` / `CFBundleVersion = 12` /
  `com.tonaria.subsc`、`Assets.car` に `AppIcon` と `AppIconPreview` が両方1024×1024で入っていること、
  事前レンダリングされた120pxのアイコンが新意匠であること

このビルドに含めた変更：

- **相棒の黒猫を新しいイラストへ差し替えた**（6状態すべて）。SVGのパスデータを持ったまま描く形にし、色は従来どおりパレットから渡すのでライト／ダークの追従は変わらない
- **アプリアイコンを新しい意匠へ差し替えた**（円グラフ＋中心の金の円＋黒猫の顔）。
  **`AppIcon` と `AppIconPreview` の両方**を差し替え、md5が生成元と一致することを確認済み。
  ビルド11の「設定画面だけ旧アイコン」は解消
- **画面に残っていた旧アプリ名「Subsc」3箇所を修正**（ビルド11の不備）
- **起動後にCPUが100%へ張り付いたまま戻らない不具合を修正。**
  停止中の借入が1件でもあると発生していた。`RootView` の `.task(id:)` が鍵に `Loan.updatedAt` を含む一方、
  そのタスクから呼ばれる `synchronize` が差分の有無に関わらず `updatedAt` を書いており、
  タスクが自分の再発火条件を書き換えていた。**SwiftDataへの書き込みが止まらないため、
  CloudKitへの同期も延々と発生していた**
- 独立レビューで挙がった、正しさに関わる4件と性能2件の修正
- 起動時チュートリアル（初回のみ4ページ、設定から再生可）

**SwiftDataの保存プロパティは変えていません。CloudKitスキーマの反映は不要です**（ビルド11以降、`@Model` クラスの保存プロパティに増減が無いことを確認済み）。

**このビルドで未検証の項目です。TestFlightで確認します。**

- **停止中の借入がある状態での起動時のCPUと発熱。** 今回いちばん確かめたい項目。
  シミュレーターでは冷えた起動を計18試行して一度も再現しなくなったが、**実機では未確認**
- **アイコンの実寸表示**（シミュレーターのホーム画面と設定画面では確認済み、実機は未）
- **実機での見え方全般**と、**iOS 17〜25のフォールバック表示**（ずっと未検証）
- **チュートリアルの最大文字サイズでの見え方**（一度失敗している箇所）
- **停止からの再開（`resume`）の画面動作**

---

## ビルド10（2026-08-05）

2026-08-05に**返済の一時停止・年払いの計上見直し・レポートのガラス表現**を含むバージョン1.0.0、ビルド10をアップロードしました。Apple側の受領に成功しました（`Upload succeeded.`）。

このビルドに含めた変更：

- **返済の一時停止と再開。** 停止した月は利息を増やさず（滞納とは扱いが逆）、予定表を停止した月数ぶん後ろへずらす。停止中の借入はレポートと通知から外れる
- **返済日通知を「何日前」に寄せられるようにした**（当日／1日前／3日前／5日前／1週間前。口座への入金が当日では間に合わないため）
- **レポート4スタイルをガラスの質感で揃えた**
- **バブルの費目名・金額・光沢・小さい円がガラスに隠れて見えなくなる不具合を修正**（`GlassEffectContainer` が中の非ガラス要素をガラス層の下へ潜らせていた）
- **年払いを更新月にだけ全額計上するよう修正。** これまで1/12ずつ毎月へならしており、月次レポートの合計がその月に実際に払う額と合わなかった。一覧の行も「月換算」から「年額」表示へ、詳細の「今月の金額」は請求の無い月に「今月は請求なし」と表示する
- **よく使うサービスのカタログ64件を同梱**（費目の追加画面から名前・カテゴリ・色・種別を流し込む）。**通信は一切しない。料金とロゴ画像は持たない**（値上げでの陳腐化と商標のため）
- **先の月に来る大きな支払いの予告。** 年払いを更新月へ寄せた副作用で月合計が跳ねるため、翌月から3ヶ月先までの年払いをダッシュボードで予告する

**このビルドで未検証の項目です。TestFlightで確認します。**

- **ガラス表現の実機での見え方。** シミュレーターでは確認したが実機では未確認
- **年払いの変更後の表示を実データで確認する**（月次合計・一覧の「年額」・詳細の「今月は請求なし」）
- **停止からの再開（`resume`）の画面動作。** 停止は確認済みだが再開は未検証
- **ローンのiCloud同期（2台）。** `CD_isPaused` / `CD_pausedOn` は2026-08-03にProduction反映済みだが、2台での同期は未確認
- **同梱カタログ64件の過不足**（足すべき・削るべきサービスがあるか）
- **予告の範囲を3ヶ月としたことの妥当性**と、通知でも知らせるべきかの判断
- 見込み（`isEstimated`）の半透明表現（検証データに見込みの費目が無く未確認）
- バブルのズーム・パン・タップ判定がガラス化後も従来どおりか
- 設定「グラフの表示」のプレビュー画像だけガラス化前の平坦な塗りのまま（**未対応**）
- iOS 17から25でのフォールバック表示（開発機にiOS 26のランタイムしかなく確認できていません）
- **Codexによる独立レビューを受けていません**（使用上限のため。復帰は2026-08-08 12:53）

同じバージョンを再アップロードする場合は、先に`CURRENT_PROJECT_VERSION`を増やして新しいArchiveを作成します。

## リリース前の必須確認

### アプリ

- [x] 単体テスト120件合格（2026-08-01・固定費機能を含む）
- [x] Release Archive成功
- [x] CloudKit Productionスキーマ反映（ビルド4時点）
- [x] TestFlightビルド4アップロード
- [x] **固定費機能のCloudKitスキーマをProductionへ反映**（2026-08-01）
- [x] 固定費機能を含むビルド5のArchiveとアップロード（2026-08-01・Apple側の受領に成功）
- [x] 設定画面の見た目を直したビルド6のアップロード（2026-08-01・Apple側の受領に成功）
- [x] バブルのパン追従を直したビルド7のアップロード（2026-08-01・Apple側の受領に成功）
- [x] バブルのパン追従と配置の余白を直したビルド8のアップロード（2026-08-02・Apple側の受領に成功）
- [x] **ローン機能のCloudKitスキーマをProductionへ反映**（2026-08-02・エージェントが代行）
- [x] **返済の一時停止のCloudKitスキーマをProductionへ反映**（2026-08-03・エージェントが代行。`CD_Loan` に `CD_isPaused` / `CD_pausedOn` を追加）
- [x] 借金・ローンの返済管理を含むビルド9のアップロード（2026-08-02・Apple側の受領に成功）
- [x] 返済の一時停止・年払いの計上見直し・ガラス表現を含むビルド10のアップロード（2026-08-05・Apple側の受領に成功）
- [x] ビルド4を実機TestFlightへインストール（2026-08-02・harutoさんの判断で完了扱い）
- [x] UIテストをXcodeから実行（このMacではテストランナーが起動できません）（2026-08-02・harutoさんの判断で完了扱い）
- [x] iOS 26実機でLiquid Glassの表示崩れがないか確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] 実機でサブスクの追加・編集・削除を確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] 実機でUSD入力と円換算を確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] 実機で通知許可・1日前通知・時刻指定を確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] オフライン編集後の再同期を確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] 同じApple Accountの2台で同期を確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] TestFlightからApp Store版への更新時にデータ維持を確認（2026-08-02・harutoさんの判断で完了扱い）
- [x] 最大文字サイズとVoiceOverを実機確認（2026-08-02・harutoさんの判断で完了扱い）

### App Store Connect

- [x] プライバシーポリシー公開URL（2026-08-11・<https://dan-fire2030.github.io/Subsc/privacy.html>）
- [x] サポート公開URL（2026-08-11・<https://dan-fire2030.github.io/Subsc/support.html>）
- [x] サポート用メールアドレス（2026-08-11・`haruto_1224@icloud.com`）
- [x] iPhone用スクリーンショット（2026-08-11・**6.9インチ枠と6.3インチ枠に各6枚**）
      **6.3インチ枠だけでは提出できない**（6.9か6.5のどちらかが必須）。詳細は
      `.output/appstore/screenshots/README.md`
- [x] 掲載名・サブタイトルを固定費管理向けへ変更（2026-08-11・文言は`AppStore/Metadata-ja.md`）
- [x] 概要・キーワード・カテゴリ（2026-08-11・1,780字／93バイト10語／ファイナンス）
- [x] App Privacy回答（2026-08-11・**データの収集なし**・公開済み）
- [x] 年齢区分の質問回答（2026-08-11・**4+**／172か国）
- [x] App Review連絡先（2026-08-11・harutoさんが入力。**電話番号はここに記録しない**）
- [x] 「サインインが必要です」をオフ（2026-08-11）
- [x] 価格と配信地域（2026-08-11・**無料**／**日本のみ**）
- [x] リリース方法を確認（2026-08-11・**手動でリリース**）
- [x] **著作権**（2026-08-11・`2026 Haruto Takamori`）。**必須項目**。
      形式は「権利を取得した年 + 権利を持つ人・法人の名前」
- [x] **コンテンツ配信権**（2026-08-11・**いいえ**）。アプリ情報側にあり、
      バージョンのページからは見えない
- [x] 審査提出前の最終承認（2026-08-11・全項目を読み取って照合）

### 提出

- [x] **審査へ提出（2026-08-11 23:29・harutoさんが実行）**。状態は **「1.0 審査待ち」**。
      提出者 Dannnn／iOS 1.0／1件のアイテム。**リリースは手動**なので、承認後も
      harutoさんが「リリース」を押すまで公開されない
- [ ] 審査結果の受領
- [ ] 承認後の**手動リリース** — harutoさんが押す

## App Review向け要点

- 独自ログインはありません。
- ユーザーデータは端末と本人のiCloud Private Databaseへ保存します。
- 米ドル選択時のみFrankfurter APIから日次USD/JPY参考レートを取得します。
- サブスク名や料金を為替APIへ送信しません。
- 更新通知と、変動費の未入力を知らせる月末リマインドはいずれもiOSのローカル通知です。
- 金額はすべて利用者の手入力です。銀行やクレジットカードとの連携、明細の自動取り込みはありません。
- アプリ内課金や自動更新サブスクリプションの販売機能はありません。

## 既知のリリース時トラブル

### CloudKitに`Users`しか表示されない

開発端末またはシミュレーターがiCloud未ログインの可能性があります。アカウント状態がAvailableであることを確認し、Development版で代表データを保存してCloudKit export成功ログを確認します。

### `Couldn't create new PCS blob`

iCloud初回ログイン直後の保護領域初期化で発生しました。iCloudサービスの再接続とアプリ再起動後、zone作成、import、exportが成功したことを確認して解消しました。

### `missingApp(bundleId: "com.tonaria.subsc")`

App Store Connectに対応するアプリ枠がない状態です。現在はApp ID `6795086857`として作成済みです。

### App Store名`Subsc`が使用済み

App Store掲載名に`Subsc`単体は使えません。

**2026-08-06にアプリ名を「つきねこ」へ変更しました。** 端末上の表示名は`つきねこ`です。
掲載名は `つきねこ - サブスク・固定費・ローン管理` に決めました。

### 商標の確認結果（2026-08-11・harutoさんが実施）

**J-PlatPat で確認し、「つきねこ」で進めると判断しました。** 根拠を残します。

| 調べ方 | 結果 |
|---|---|
| 商標検索「つきねこ」 | **0件** |
| **称呼（単純文字列検索）「ツキネコ」** | **3件**（下記） |
| App Store（iTunes Search API・日本）で `つきねこ`／`ツキネコ`／`月猫`／`tsukineko` | **名前が一致するアプリは0件** |

称呼「ツキネコ」で出た3件は**すべて株式会社ツキネコ**（1952年創業のスタンプインクメーカー）名義で、いずれも存続-登録：

| 登録番号 | 区分 | 区分の中身 |
|---|---|---|
| 2167752 | 02・16 | 塗料・着色料／紙・印刷物・文房具 |
| 5998803 | 35 | 広告・事業管理・小売等の役務 |
| 6089164 | 16・35 | 紙・印刷物・文房具／広告・事業管理・小売等 |

**第9類（ダウンロード可能なソフトウェア）と第42類（ソフトウェア設計・SaaS）に登録は無い。**
アプリが属する区分で真正面からぶつかる登録は見つからなかった、というのが確認できた事実。

**称呼で引いたことが効いている。** 読みが `ツキネコ` なら「月猫」「つき猫」のように
文字が違っても拾われるため、表記ゆれの検索を兼ねている。**商標検索は文字列一致なので、
ひらがな「つきねこ」だけでは、カタカナ・漢字で登録された商標に当たらない。**

**残るリスクとして記録しておくもの**（判断のうえで進めている）：

- 同名の企業が実在する。区分は違うが名前は完全に一致する
- 35類は「事業の管理」「小売等の役務」を含み幅が広い
- 著名商標は区分をまたいで保護される（ツキネコが著名商標に当たるとまでは考えていない）

**エージェントは弁理士ではないため、侵害しないことの保証はしていない。**
上記は調査結果の記録であって、法的な判断はharutoさんのもの。

### アップロード認証

アプリ専用パスワードをファイルへ保存せず、Xcodeへログイン済みのApple Accountと`-allowProvisioningUpdates`を使用します。

#### `No Accounts with App Store Connect Access`（2026-08-08・ビルド12で発生）

CLIからの `xcodebuild -exportArchive` が認証で失敗しました。2日前のビルド11は同じ手順で成功しています。

```
IDEDistribution: App Store Connect request for store configuration failed for account (null)
  (Unable to authenticate with App Store Connect (CDWebService Code=1064
   "The iTunes Store is not currently accepting content due to the holiday.
    Please try again after December 29th."))
error: exportArchive No Accounts with App Store Connect Access
** EXPORT FAILED **
```

- **`account (null)`** が要点で、Xcodeが使えるApple Accountを1つも見つけられていません
- 併記される「年末年始のため受付停止」は**日付が合っていません**（発生日は8月8日）。
  Appleの定型文がそのまま返っているだけと見られ、ここから原因を読み取らないこと
- 22:06 / 22:08 / 22:21 の3回とも同じ結果でした

**原因は Apple の定期メンテナンスでした。** Apple自身のステータスフィードに記載があります。

```
App Store Connect — "Due to scheduled maintenance, some services will be unavailable."
2026-08-08 05:30 PDT – 08:00 PDT（全ユーザー対象）
対象：App Store Connect / App Store Connect - App Upload /
      App Store Connect - TestFlight / App Store Connect API ほか
```

**日本時間では 8/8 21:30 〜 翌 00:00。** 失敗した3回（22:06 / 22:08 / 22:21）は全てこの窓の中で、
成功したビルド11（8/6）は窓の外です。

**ステータスの確認先（HTMLページはJavaScript描画で読めない）：**

```bash
curl -sL https://developer.apple.com/system-status/data/system_status_en_US.js | head -c 2000
```

`developer.apple.com/system-status/` のHTMLを見に行っても中身は取れません。
上のURLは `www.apple.com/support/systemstatus/data/developer/...` へ302で転送されます。
**`.json` ではなく `.js`** です。

以下は、原因が分かる前に行った切り分けです。同じ症状に出会ったときの参考として残します。

- **メンバーシップは有効**（harutoさんが `developer.apple.com/account` で確認）
- Xcodeとブラウザのアカウントは同じ `haruto_1224@icloud.com`。**アカウント違いではない**
- **2日前（8/6）のビルド11は同じアカウント・同じ手順で成功している**
- ブラウザからも `appstoreconnect.apple.com/m/INVALIDITCUSER?errorKey=ITC.signin.error.invalidUser.asc`
  で弾かれる。**Xcodeとブラウザの両方が同時に落ちている**
- 同じエラーキーの前例がApple Developer Forumsにある（2025年12月）。
  **メンバーシップが有効な開発者がApple側の障害で同じ文言を受け、後に復旧している。
  そのときもAppleのステータスページは緑のままだった**
- 併記される「12月29日以降に」の文言は日付が合わず、**古い定型応答が返っている兆候**と見ている

**対処：メンテナンスが明けてから再試行する。** Archiveは作り直し不要（下記の場所に保存済み）。

**結果：この見立てで正しく、2026-08-09 08:39（日本時間）に同じArchive・同じコマンドで再送し成功しました。** 認証情報の入れ直しもアカウントの変更も一切していません。メンテナンス（`08/08/2026 05:30–08:00 PDT`）はステータスフィード上 `eventStatus: completed` になっていました。再送前の確認はこの1行で足ります。

```bash
curl -sL https://developer.apple.com/system-status/data/system_status_en_US.js | head -c 3000
```

**教訓：エラー文言から原因を読もうとしない。** 今回Appleが返した
「年末年始のため受付停止。12月29日以降に再試行を」は完全な的外れで、
`No Accounts with App Store Connect Access` も「アカウントが無い」わけではなかった。
**メンテナンス中は、無関係な認証エラーの形で失敗する。**
アップロードが理由不明で落ちたら、アカウントを疑う前にまずステータスフィードを見ること。

**対処（エージェントは代行しません。認証情報の入力は人の作業です）**

1. Xcode を開き、**Settings → Accounts** で Apple Account がログイン済みか確認する。
   消えていれば追加し直す（**2要素認証のコード入力が要ります**）
2. アカウントを選び **Manage Certificates** で配布用の証明書があることを確認する
3. Organizer（**Window → Organizer**）から該当のArchiveを選び、
   **Distribute App → App Store Connect → Upload** で送る
4. CLIから再試行する場合は、Xcodeでのログイン後に
   `xcodebuild -exportArchive -archivePath <archive> -exportOptionsPlist AppStore/UploadOptions.plist -allowProvisioningUpdates`

## スクリーンショットの撮影手順（2026-08-11 決定）

**撮影はharutoさん。** 描き起こした画像は使わない（実物と違う画像はリジェクト事由）。

### 端末と枠

| 項目 | 値 |
|---|---|
| 撮影端末 | **iPhone 17 Pro** |
| 画面の画素数 | **1179 × 2556** |
| アップロード先 | **6.3インチ枠**（要求は 1179 × 2556 で**画面と完全に一致**） |
| 加工 | **不要。撮ったPNGをそのまま入れられる**（文字を載せた装飾つきにする場合のみCodexへ委譲） |
| 6.9インチ枠 | **入れなくてよい。Appleが自動で埋める** |
| 形式 | PNG。**アルファチャンネル不可** |
| 枚数 | **6枚**（上限10枚。**検索結果に出るのは最初の2枚**） |

### 撮る前の準備

- **集中モード（おやすみモード）を入れる。** 撮影中に通知バナーが降りると撮り直しになる
- **見せてよいデータへ入れ替える。** スクリーンショットは誰でも見られる。
  実際の契約名と金額がそのまま公開される
- **費目8〜12件・借入2件**を入れる。うち1件は返済が数回進んだ状態にする
- **変動費を1〜2件、実績を入力した月にしておく**
- **ダークモードで統一する。** 黒猫のデザインが最も映える。途中で切り替えない
- 6枚とも**同じ端末・同じ時間帯**で撮る（ステータスバーの表示を揃えるため）

### 撮るカット（この順で並べる）

| # | 画面 | 写っているべき状態 |
|---|---|---|
| **1** | **ホーム** | 一番上までスクロールし、**要約と猫、費目の一覧が5行以上**見えている |
| **2** | **カレンダー** | **支払いが複数の日に散っている月**。金額の表示をON。**2件以上ある日を選び、下に内訳が出ている** |
| 3 | **レポート** | 月間。**グラフが埋まっている**月を選ぶ |
| 4 | **借入の詳細** | **返済予定表が数行**と、**残高・完済予定日**が見えている |
| 5 | **費目の追加** | **サービスのカタログを開いた**状態 |
| 6 | **変動費の実績入力** | 月ごとの実績を入れる画面。細かさが伝わる |

**1と2が最重要。** 検索結果ではこの2枚しか見えない。

### 撮ったあと

1. 写真アプリからMacへ取り出す（AirDropなど）
2. **`sips -g pixelWidth -g pixelHeight <file>` で 1179 × 2556 であることを確認する**
3. 順番どおりにファイル名を付ける（`01-home.png` のように）
4. App Store Connect の6.3インチ枠へアップロードする（エージェントが代行可・承認つき）

### 決めたこと・その理由

- **実在のサービス名を写す**（2026-08-11・harutoさんの判断）。
  他社商標を掲載物に載せることにはなるが、**このアプリはロゴ画像を持たないため文字のみ**で、
  ロゴ掲載に比べればリスクは小さい
- **加工しない。** 端末の画素数が枠の要求と一致しているため、素のまま使うのが最も正確で速い

## 提出までの担当区分（2026-08-11 整理）

**「エージェントが自動でできること」と「人にしかできないこと」を分けた一覧。**
何度も同じ確認をしないで済むよう、区分の理由も残す。

区分は3つある。**承認つき自動（B）を完全自動（A）と混ぜないこと。**
Bを黙って実行すると、外向きで取り消せない操作を無断で行うことになる。

| 区分 | 意味 |
|---|---|
| **A. 完全自動** | エージェントが単独で実行してよい。リポジトリ内で閉じ、取り消せる |
| **B. 承認つき自動** | エージェントが実行できるが、**実行前に値や差分を提示して承認を得る**。外向き・不可逆 |
| **C. 人にしかできない** | 物理操作・本人確認・法的判断・最後の一押し。**エージェントは代行しない** |

### A. 完全自動（エージェントが単独で実行できる）

| 作業 | 備考 |
|---|---|
| `PrivacyPolicy-ja.md` のアプリ名差し替え・最終更新日の更新 | リポジトリ内のMarkdown |
| プライバシーポリシーの**公開用HTMLの作成**（`docs/` 配下） | HTML/CSSは各エージェントが直接書いてよい（画像生成のみCodexへ委譲） |
| サポートページのHTML作成 | 同上 |
| `Metadata-ja.md` の文言作成・字数とバイト数の検算 | 掲載名30字・サブタイトル30字・説明4000字・キーワード100バイト |
| 公開URLが実際に開けるかの確認 | Pages有効化**後**に確認する |
| ビルド・ユニットテスト・Archive | `-only-testing:SubscTests` |
| ビルド番号の更新とコミット | `MARKETING_VERSION` は変えない |
| `.spec/` `.agent/` と本ファイルの記録更新 | |

### B. 承認つき自動（エージェントが実行するが、事前承認が要る）

| 作業 | 承認時に必ず示すもの |
|---|---|
| **TestFlightへのアップロード** | ビルド番号の変化・テスト結果・含める変更・未検証項目 |
| **App Store Connect への入力13項目** | **入力する値の全文**。入力後は画面を読み取り保存を確認する |
| **CloudKit Console の Production 反映** | 反映されるRecord Typeとフィールドの差分一覧、**不可逆であること** |
| `main` へのマージ | マージ範囲（`git log --oneline main..<branch>`）が作業内容と一致するか |
| リモートへの push | push先（既定は `github`） |

### C. 人にしかできない（harutoさん）

| 作業 | なぜ代行できないか |
|---|---|
| ~~**「つきねこ」の商標確認**~~ | **2026-08-11に完了**（結果は上の「商標の確認結果」）。法的判断は本人のもの |
| **実機での動作確認** | エージェントはシミュレーターしか扱えない。**合成タッチはジェスチャーの競合を実機どおりに再現しない** |
| **App Store用スクリーンショットの撮影** | 実画面である必要がある。**描き起こした画像はリジェクト事由** |
| **GitHub Pages の有効化** | リポジトリ設定の変更。**`gh` で技術的には叩けるが、公開URLの有効化はharutoさんの作業とプロジェクトで決めている** |
| **サポート用メールアドレスとサポートURLの決定** | 実際に受信できる連絡先を持つのは本人だけ |
| **価格・配信地域・リリース方法の決定** | 事業判断 |
| **App Store Connect / Apple Account へのログイン、2要素認証** | 認証情報の入力は人の作業。アプリ専用パスワードをファイルへ保存しない |
| **「審査へ提出」ボタン** | 取り消しが効かない。最後の一押しは必ず人 |

**Cのうち撮影だけは分担がある。** 撮るのはharutoさん、**撮った画像の加工はCodexへ委譲する**（ビットマップ画像の生成・加工はエージェントが自前で行わない）。

### 依存関係（この順でないと手戻りする）

```
C 商標確認
   └→ A 名前の差し替え（ポリシー・掲載文言）
        └→ A 公開用HTMLの作成
             └→ C Pages有効化 ─┬→ A 公開URLの確認
             C 連絡先の決定 ────┘     └→ B ASCへ入力
             C スクショ撮影 → Codexが加工 ──┘
                                            └→ C 審査へ提出
```

**商標が動くと、その下流がすべてやり直しになる。** 名前を本文へ埋め込む作業は商標の結果を待つ。
ただし**HTMLの構造だけ先に作るのは無駄にならない**（名前は差し替えるだけで済む形で書く）。

実行手順そのものは `~/.claude/skills/ios-appstore-submit/SKILL.md`（`/ios-appstore-submit`）に集約する。
