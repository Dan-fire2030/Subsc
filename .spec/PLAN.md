# PLAN - やりたいこと（自由記述）

## 2026-08-03 haruto さんからの依頼

- **ローンの一時停止機能**をやりたい（前サイクルからの持ち越し。CloudKit再反映が要るので保留していた）
- 追加で、**バブル表示の見た目をガラスっぽく、ふわふわ浮いてるような感じ**にしたい

`AskUserQuestion` で2ラウンド確認し、仕様を確定させた。

## ヒアリング済みの決定（2026-08-03）

### 一時停止

- 停止の意味は**期日を後ろへずらすだけ**。利息は増やさない
- 保存は `isPaused` ＋ `pausedOn` の2フィールド（**CloudKit Production 再反映＝不可逆**）
- レポート・通知は**サブスクの停止と同じ**扱い。停止前の実績は過去のレポートに残す
- 操作は**詳細画面のボタン**と**一覧のスワイプ**の両方
- 一覧では**薄くして「停止中」バッジ**

### グラフのガラス化

- **色を残したまま半透明ガラス**（シャボン玉には寄せない。費目の色分けを保つ）
- **上下＋左右にゆっくり漂う**
- **4スタイル全部**（バブル／棒／積み上げ／同心円）を揃える

## 前提・注意

- ベースは `feat/loan-repayment`（**main へ未マージ、23コミット**）
- 一時停止は不可逆な CloudKit 反映を伴うため、ガラス化とはブランチを分ける
- ビルド9には未反映の修正が8件溜まっている。本サイクルの成果はビルド10へまとめる
- Codex の独立レビューは使用上限で未実行（**復帰は 2026-08-08 12:53**）
