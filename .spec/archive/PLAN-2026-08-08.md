# PLAN - やりたいこと（自由記述）

## 2026-08-05 haruto さんからの依頼

- **UIを超大幅に変更する。** アプリ全体を作り直す
- モチーフは **めっちゃスタイリッシュでおしゃれな黒猫**
- 今のUIに不満があるわけではない。**デザイン全体の張り替え**が目的

## ヒアリング済みの決定（2026-08-05）

- **範囲：アプリ全体。** タブ構成・ナビゲーションを含む全画面。情報設計から見直す
- **黒猫は相棒キャラとして登場する。常に居て、状態に反応する**
  （支出が多い月は心配そうにする等。ダッシュボードに常駐）
- **ライト・ダーク両対応。** ダーク固定にはしない
- **アプリアイコンもアプリ名も変える。** ただし**名前は後回し**。
  画面のデザインを先に進め、世界観が見えてから決める

## 前提・注意

- ベースは `main`（ビルド10まで出荷済み・作業ツリーはクリーン）
- **Bundle ID `com.tonaria.subsc` は変えられない。** CloudKitコンテナと
  App Store Connect App ID `6795086857` が紐づいているため。
  変えられるのは端末上の表示名とApp Storeの掲載名だけ
- **ビットマップ画像（アプリアイコン・マスコットのイラスト）は今すぐ作れない。**
  本リポジトリのルールで画像生成はCodex委譲が必須だが、Codexは使用上限で
  **復帰が 2026-08-08 12:53**。SwiftUIで描くベクター表現とSF Symbolsは今すぐ可能
  → **デザイン言語の構築を先に、ビットマップ資産は8/8以降**
- **「クロネコ」はヤマト運輸の登録商標。** 名前に含めるのは避ける
- CloudKitのRecord Type名（`Subscription` / `Loan` / `LoanPayment`）は変えない。
  **今回はUIだけの変更で、保存されるデータの形は変えない**のが原則
- Codex の独立レビューは使用上限で未実行（**復帰は 2026-08-08 12:53**）

## まだ決まっていないこと

- アプリ名とアイコン
- 猫の見た目（画風・線の太さ・目の色）と、状態ごとの表情の種類
- 画面構成をどこまで変えるか（タブを増やす／減らす、ダッシュボードの構造）
- 既存のテーマ色設定・グラフ4スタイル設定を残すのか、新しい体系へ畳むのか
