# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（レポートカードの可視化スタイル／2026-08-01 確定）

## 前サイクルからの繰り越し

**完了していないまま新サイクルへ移ったもの。**

### 固定費の記録（`feat/cost-type-data-model`・15コミット）

- [x] **CloudKit の Production へスキーマを反映**（不可逆・haruto さんの操作）（2026-08-02・harutoさんの判断で完了扱い）
      Development 側は生成確認済み。`CD_AmountEntry` と `CD_Subscription` の5フィールド追加
- [x] main へマージ（2026-08-02・harutoさんの判断で完了扱い）
- [x] ビルド5の Archive とアップロード、`RELEASE_RUNBOOK.md` への追記（2026-08-02・harutoさんの判断で完了扱い）
- [ ] App Store Connect へ掲載名・サブタイトル・概要・キーワードを入力

### 収益化（リリース3ヶ月後に判断・2026-08-02決定）

- [ ] リリースから3ヶ月後、App Store Connect の継続率とダウンロード数を確認する
- [ ] 継続率が立っていれば、CSV/PDFエクスポートと年間レポートの詳細分析を
      **買い切りのPro解放**として実装する（¥800〜1,200想定）
- [ ] ホーム画面ウィジェットは**無料**で実装する（起動頻度を上げるため）

### テーマ色（`feat/theme-color`・8コミット）

- [x] **iOS 17〜25 のフォールバック表示の確認**（`glassEffect` が効かない系統）（2026-08-02・harutoさんの判断で完了扱い）
      このMacには iOS 26 のランタイムしか無い。**リリース前には必ず潰すこと**
- [x] iOS 26 実機での確認（**追加機能を入れてからまとめて実施**と決定）（2026-08-02・harutoさんの判断で完了扱い）
- [x] 見送ったレビュー指摘（詳細は `.spec/archive/TODO-2026-08-01-chart.md`）（2026-08-02・harutoさんの判断で完了扱い）
      iOS 26のGlass透過／ボタン色のコントラスト／補正の説明文言／Dynamic Type／
      無反応のサンプルボタン／デバウンス／プリセット共用

### さらに別サイクルへ

- [x] 停止中にすると停止前の実績まで過去レポートから消える（Codexレビュー Medium 6）（2026-08-02・harutoさんの判断で完了扱い）
- [x] `SettingsView` に変動費のリマインドを一括オン／オフする設定を置くか検討（2026-08-02・harutoさんの判断で完了扱い）
- [x] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する（2026-08-02・harutoさんの判断で完了扱い）
- [x] `SubscApp.swift` の通常時の `ModelContainer` に `AmountEntry` が明示されていない（2026-08-02・harutoさんの判断で完了扱い）
      （リレーション経由で引き込まれるため現状は動くが、外した瞬間に静かに壊れる）

## 本サイクル：レポートカードの可視化スタイル

### フェーズ1：データの土台（TDDで進める）

- [x] `CostType` に色を持たせる（`title` / `systemImage` と同じ流儀で型側に置く）
- [x] **先にテストを書く**：`ReportEntry` に `costType` を持たせ、`ReportCalculator` が詰めること
- [x] `ReportEntry` へ `costType` を追加する
- [x] **先にテストを書く**：種別ごとの集計（金額の合計、見込みが1件でもあれば見込み扱い、
      0円の種別を出さない、0件・1件、全件同額）
- [x] 種別ごとの集計を実装する
- [x] **先にテストを書く**：バブルの配置（面積が金額に比例する、重ならない、
      初期倍率で全件が収まる）
- [x] バブルの配置計算を実装する（ビューから切り離した純粋な計算にする）

### フェーズ2：スタイルの選択と保存

- [x] `ReportChartStyle` enum（`bar` / `ring` / `bubble` / `column`）を追加する
- [x] `ThemeStore` へ保存する（既定は `bar`。`resetToDefaults` の対象に含める）
- [x] **CloudKitのスキーマを変更していないことを確認する**（`UserDefaults` のみで完結）

### フェーズ3：4つのスタイルの実装（完了）

- [x] 共通：色の決め方を `ReportChartPalette` へ集約（**集計単位は期間で決まる**）
- [x] 共通：見込みを半透明で描き、VoiceOverで「見込み」と読ませる
- [x] A. 積み上げ1本帯
- [x] B. 同心リング
- [x] C. バブル（ピンチズーム、ダブルタップで初期倍率、**等倍ではパンしない**）
      領域いっぱいの配置／漂う動きと登場アニメーション／小さい円のタップで吹き出し
- [x] D. 縦バー（横スクロール・慣性、端のフェード、収まるときはスクロール無効）
- [x] どのスタイルでもタップで `ReportBreakdownSheet` を開けること
      （バブルは「ラベルの入らない小さい円 → 吹き出し」「それ以外 → シート」に切り分け）

### フェーズ4：設定画面（完了）

- [x] 設定画面へ「グラフの表示」を追加
- [x] 選択画面（4つ＋カードと同じ下地の上でのプレビュー）
- [x] 「既定に戻す」でスタイルも戻る（文言も「既定に戻す」へ変更）

### フェーズ5.5：ビルド7で見つかった不具合の修正（2026-08-02）

TestFlightビルド7の実機確認で「前回の修正が効いていない」と報告を受けた分。

- [x] **拡大中のドラッグが指に追従しない／月が切り替わる**（`f9f3349`）
      ビルド7の `highPriorityGesture` は的外れだった。優先度指定では
      横スクロールのパン認識器に勝てないため、拡大中は `scrollDisabled` で止める
- [x] **横に約2割の余白が残る**（`075a4b0`）
      等方縮小の残りを、半径を変えず中心の間隔だけ広げて埋める
- [x] ユニットテスト全件（2026-08-02 / 185件成功。実機の縦横比に近い300×130の
      ケースを追加し、余白95%以上・非重複・はみ出しなしを縛った）
- [x] **実機で3点を確認**（ハルトさん。シミュレーターでは合成タッチのため再現しない）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）

### フェーズ5：検証

- [x] ビルドとユニットテスト全件（2026-08-01 / 184件成功）
- [x] 4スタイルの表示をシミュレーターで目視確認
- [x] **カードの慣性スライドが壊れていないこと**（実機で確認）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）
- [x] **Cが等倍でページ送りでき、ズーム中はパンすること**（実機で確認）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）
- [x] **Dの横スクロール中にページが変わらないこと**（実機で確認）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）
- [x] バブルの漂う動き・登場アニメーション・吹き出しの出方（実機で確認）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）
- [x] 費目0件・1件・全件同額・1件だけ極端に大きい場合の見た目（**未確認**）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）
- [x] アクセシビリティ文字サイズ（グラフ領域340pt）での4スタイル（**未確認**）（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）
- [x] 独立レビュー — **実施せず完了扱いとする**（2026-08-02・harutoさんの判断）。
      レビュー自体は行っていないので、記録としてはここに残す
- [x] iOS 26 実機での確認（2026-08-02・harutoさんの判断で完了扱い。**実機確認そのものは行っていない**）

## 完了済み

（このサイクルではまだなし。過去分は `.spec/archive/` を参照）
