# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（テーマ色／2026-08-01 確定）

## 前サイクル（固定費の記録）からの繰り越し

**完了していないまま新サイクルへ移ったもの。** 詳細は `.spec/archive/TODO-2026-08-01.md` と
`ios/Subsc/AppStore/RELEASE_RUNBOOK.md` を参照。

- [ ] シミュレーターでの目視確認（登録・記録・レポート・絞り込み）※ haruto さんが実施中
- [ ] **CloudKit の Production へスキーマを反映**（不可逆・haruto さんの操作）
      Development 側は生成確認済み。`CD_AmountEntry` と `CD_Subscription` の5フィールド追加
- [ ] `feat/cost-type-data-model`（15コミット）を main へマージ
- [ ] ビルド5の Archive とアップロード、RELEASE_RUNBOOK への追記
- [ ] App Store Connect へ掲載名・サブタイトル・概要・キーワードを入力（haruto さんの作業）

## 別サイクルへ送ったもの（着手しない）

- [ ] 停止中にすると停止前の実績まで過去レポートから消える（Codexレビュー Medium 6）。
      「いつからいつまで利用中だったか」を保存するモデルが必要で、SPECにない追加要件のため
- [ ] `SettingsView` に、変動費のリマインドをまとめてオン／オフする設定を置くか検討
- [ ] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する

## 本サイクル：テーマ色

### フェーズ1：色のモデルと変換ロジック（TDDで進める）

- [x] 既存の色定義を1箇所へ寄せる。`SubscriptionFormView.swift` の `colorPresets` と
      `SubscriptionFormView+CategoryColor.swift` の名前定義を共用できる型にまとめる
      （**既存の見た目と挙動を変えないこと**。純粋な移動にとどめる）
- [x] **先にテストを書く**：HSB変換の往復、極端な入力（真っ白・真っ黒・彩度0）で破綻しないこと
- [x] 16進文字列 ↔ 色 の変換に既存 `ColorHex` を流用できるか確認し、足りなければ拡張する
- [x] **先にテストを書く**：カード色の補正（明度・彩度を白文字が読める範囲へ収める）の下限・上限
- [x] 補正ロジックを実装する
- [x] **先にテストを書く**：1色から3色グラデーションを生成する（色相を左右へずらす）
- [x] グラデーション生成を実装する

### フェーズ2：設定の保存と読み出し

- [x] `@AppStorage` でボタン色・カード色を保存する型を追加する（既定値は現在の色）
- [x] **CloudKitのスキーマを変更していないことを確認する**（Console の Development に差分が出ない）
- [x] 既定へ戻す操作を実装する

### フェーズ3：適用

- [x] ボタン色を `App/RootView.swift:41` の `.tint(...)` へ適用する
- [x] カード色を `ReportGlassStyle.swift` の `LiquidGlassCardBackground` へ適用する
- [x] `glassEffect` のtintと影の色を連動させる
- [x] 画面背景（`AppLiquidGlassBackground`）を連動させる
- [x] 設定画面ヘッダー・空状態・`DashboardView`・`NotificationTimingView` を連動させる
- [x] **状態色（利用中/停止中の緑・オレンジ、削除の赤）を変えていないこと**を確認する

### フェーズ4：設定画面

- [x] 設定画面に「テーマ」セクションと2項目を追加する
- [x] 色選択画面（プリセット一覧＋「その他の色」の `ColorPicker`）を作る
- [x] 選択結果のプレビューを出す（カード色はレポートカードの縮小版）
- [x] 「既定に戻す」を置く

### フェーズ5：検証

- [x] ビルドとユニットテスト全件（2026-08-01 / 149件成功）
- [x] シミュレーターで目視確認（haruto さんが実施・問題なし）
- [x] Codex による独立レビュー（9件の指摘。下記「レビューへの対応」を参照）
- [x] レビュー指摘の High 1件と Medium 1件に対応（`ad3511a`）
- [ ] **iOS 17〜25 のフォールバック表示**の確認（`glassEffect` が効かない系統）
      **2026-08-01：次回に持ち越すと決定。** このMacには iOS 26 のランタイムしか無く、
      確認するには古いランタイムのダウンロード（数GB）が要るため。
      **1.0.0 は iOS 17 以降が対象なので、リリース前には必ず潰すこと**
- [ ] iOS 26 実機での確認
      **2026-08-01：追加機能を入れてからまとめて実施すると決定**（haruto さん）

### レビューへの対応（2026-08-01）

対応した：

- [x] **[High] グラデーションの2色目・3色目が無補正**で、白文字のコントラストが不足していた
      （既定色の3色目 3.15:1、ブルー 2.62:1）。各停止色にも個別に補正をかけるよう修正
- [x] **[Medium] `ThemeStoreTests` が `ThemeColor` の静的関数を直接呼んでいた**ため、
      画面側から補正が消えてもテストが通る状態だった。`store.cardBaseColor` 経由へ変更

見送った（理由つき）：

- [ ] **[High] iOS 26 の Glass 透過で最終コントラストが保証されない**。指摘の論理は正しいが、
      `.opacity(0.82)` と `glassEffect` は**変更前から同じ**で今回持ち込んだものではない。
      手を入れると既存デザインの作り変えになるため別で扱う
- [ ] **[Medium] ボタン色のコントラスト**（グリーン 2.22、オレンジ 2.06）。
      **見送ると haruto さんが判断**（元の既定色も 4.32 で文字基準に届かず、
      Apple標準の systemBlue も同程度のため）
- [ ] [Medium] 補正が起きたことを利用者に説明する文言がない
- [ ] [Medium] 最大文字サイズでカードプレビューが切れる可能性（高さ120pt固定）
- [ ] [Low] ボタン色プレビューのサンプルボタンが押しても無反応（VoiceOverが操作可能と読む）
- [ ] [Low] `ColorPicker` のドラッグ中に保存と色変換が毎回走る（デバウンス／キャッシュ）
- [ ] [Low] プリセット定義を費目フォーム側と共用できていない（見た目を変えるリスクがあるため別途）

## 完了済み

（このサイクルではまだなし。過去分は `.spec/archive/TODO-2026-08-01.md` を参照）
