# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（起動時チュートリアル・2026-08-08 確定）
ブランチ：`feat/onboarding-tutorial`（ベースは `design/black-cat-redesign`）

---

## 今サイクルで完了（2026-08-08）

- [x] **黒猫マスコットとアイコンの検討成果を保全**（`655ffab`）
      `.output/design-system/brand/` に40ファイル。採用・不採用の別は同ディレクトリの
      `README.md` に明記した
- [x] **猫6状態を SwiftUI へ移植**（`1d1cf10`）
      SVGのパスデータを持ったまま `CatPathParser` で読む形にした。
      色はハードコードせず `BlackCatPalette.cat` / `.catEye` を渡している。
      ライト・ダーク両方で、ソースSVGと一致することを実機画面で確認済み
- [x] **起動後にアプリが無限に回るのを止めた**（`3193508`）
      **猫の作業とは無関係な既存の不具合**。詳細は `.spec/KNOWLEDGE.md`

---

## 前サイクルからの繰り越し（黒猫リデザイン）

- [ ] **アプリアイコンの差し替え**（`AppIcon.appiconset` と `AppIconPreview.imageset` の**両方**）
      原本は `.output/design-system/brand/app-icon-v2.svg`。
      1024×1024・アルファなしのPNG化は**Codexへ委譲する**。
      **ビルド11に旧アイコンが混入している**
- [ ] **TestFlight ビルド12**（上のアイコンを入れてから）
- [ ] **実機での確認**（harutoさんの作業）
      iOS 17〜25のフォールバック表示／アイコンの29pt表示／空状態の案内の猫／
      返済予定表・履歴／検索／金額入り通知の実文面／
      **停止中の借入がある状態での起動**（無限ループを直した箇所）
- [ ] **main へのマージ**（harutoさんの判断で**実機確認の後**）
- [ ] **「つきねこ」の商標確認**（J-PlatPat）
- [ ] `PrivacyPolicy-ja.md` のアプリ名差し替え（**公開済みURLの文書。タイミングを決める**）
- [ ] Codexによる adversarial review（レビューは自分のコードを自分で読んだため）
- [ ] 同梱サービスカタログ64件の過不足の判断（harutoさん）
- [ ] 支払い予告の範囲を3ヶ月とした妥当性、通知でも知らせるかの判断
- [ ] App Store Connect の掲載情報入力（掲載名は `つきねこ - サブスク・固定費・ローン管理` に決定済み）
- [ ] App Store用スクリーンショットの撮影（撮影はharutoさん、加工はCodex）

---

## 検索して見つけた項目まで到達できるようにする（2026-08-09・`feat/search-scroll-to-item`）

harutoさんの依頼「検索したら、その項目のとこまで移動するようにしたい」への対応。

- [x] **候補をタップしても絞り込まれない不具合を直した。**
      `query = item.name` の直後に `dismissSearch()` を呼んでいたが、
      `dismissSearch` は**検索フィールドの文字を消す**と規定されている。
      Appleが用意している `searchCompletion` へ差し替えた
- [x] **検索中は一覧より上のセクションを畳む。**
      一覧は画面のいちばん下にあり、絞り込んでも結果は画面外だった。
      レポート・大きな支払いの予告・これから出ていく・状態のピッカーを検索中だけ隠す
- [x] **検索中は状態（利用中／停止中／履歴）の絞り込みを外す。**
      候補は元から状態を見ないため、一覧だけ絞ると
      「候補に出たのに選ぶと見つかりません」になっていた。
      ピッカーを隠す以上、効いたままだと理由が画面に残らない
- [x] 見出しを「検索結果」に切り替える（`DashboardListHeading`）
- [x] ユニットテスト9件を追加。既存を含め464件が通る
- [ ] **実機／シミュレーターでの見え方の確認**（harutoさんの作業）

---

## 起動時チュートリアル（フェーズ1〜4）

**実装は `cee56a0` で完了しているが、この一覧のチェックは当時つけられていない。**
どの項目がどこまで確認済みかを個別に裏取りしていないため、チェックは伏せたままにする。
状況は `.agent/handoff/HANDOFF.md` の記録を参照すること。

## フェーズ1：表示要否のストア（TDD）

**先にテストを書く。** 実行日に依存させない。

- [ ] `OnboardingStore`（`UserDefaults` 注入・`@Observable`）
      既存の `ThemeStore` / `LoanNotificationSettings` と同じ流儀に揃える
- [ ] テスト：初回は出す
- [ ] テスト：完了すると次から出ない
- [ ] テスト：スキップも完了と同じ扱い
- [ ] テスト：見直しても「見た」記録は消えない
- [ ] テスト：保存値が壊れていても起動を止めない

## フェーズ2：チュートリアル画面

- [ ] 1ページぶんのビュー（見出し・本文・絵・ドット）
- [ ] 4ページの内容を流し込む（費目の登録／借入と返済予定表／レポートの見方／通知とデータ）
- [ ] スキップと「はじめる」
- [ ] **1ページ目にだけ猫を「案内」の姿で座らせる**（`CatMood.guiding`）
- [ ] 最大文字サイズでページ内をスクロールさせる（**固定高さで `clipped()` しない**）
- [ ] `reduceMotion` でページ送りのアニメーションを外す

## フェーズ3：組み込み

- [ ] `RootView` へ差し込む（初回のみ被せる）
- [ ] `SettingsView` に「チュートリアルをもう一度見る」を足す
- [ ] pbxprojへ新規ファイルを登録する（**次の空き番は 161。`+` を含むパスは引用符が要る**）

## フェーズ4：確認

- [ ] ビルドとユニットテストが通る（**既存テストは落ちてはいけない**）
- [ ] ライト・ダーク両方で確認
- [ ] 最大文字サイズで確認
- [ ] **CloudKitスキーマの変更が無いことを確認する**（`@Model` に触っていないこと）
- [ ] 独立レビュー（Codex復帰後なら `/codex:rescue`）
