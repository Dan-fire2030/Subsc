# TODO - タスクリスト

最終整理：2026-08-09
ブランチ：**`main`**（黒猫リデザインからビルド15までを 2026-08-09 に fast-forward でマージ済み）
現在のビルド：**15**（識別情報の唯一の情報源は `ios/Subsc/AppStore/RELEASE_RUNBOOK.md`）

前サイクル（黒猫リデザイン／起動時チュートリアル）の一覧は `.spec/archive/TODO-2026-08-09.md` にある。
**チェックが付いていない項目が多く残っているが、大半は実態としては完了済み**だった。
記録と実態がずれたまま持ち越すと次の判断を誤るため、ここで実態に合わせて作り直した。

---

## 残タスク

### A. 実機で未確認（harutoさんの作業）

エージェントはシミュレーターまでしか扱えない。**確認はharutoさんに依頼する。**

- [ ] **停止中の借入がある状態での起動時のCPUと発熱** ← **最優先**
      ビルド12で直した無限ループそのもの。シミュレーターでは冷えた起動を計18試行して
      再現しないが、**実機は一度も確認していない**
- [ ] アプリを**完全終了した状態**での、返済日通知の「返済した」ボタン
      ビルド15でデリゲートの割り当てを `init` へ早めた箇所。開いたままでは元から動く
- [ ] 検索・ダークでの猫・チュートリアルの実機での見え方（シミュレーターでは確認済み）
- [ ] アイコンの実寸表示（29pt、明るい／暗い壁紙）
- [ ] **iOS 17〜25のフォールバック表示**（ずっと未検証。iOS 26以外の端末が要る）
- [ ] 停止からの再開（`resume`）の画面動作

### B. 審査提出のブロッカー

**3の商標が最上流。** 名前が動くと 1・2・4・5 がすべてやり直しになる。

- [ ] **1. プライバシーポリシーを公開する**
      `gh api repos/Dan-fire2030/Subsc/pages` が404で、**GitHub Pages が未有効化**。
      公開URLは審査提出の必須項目。文書とHTMLの用意はエージェント、有効化はharutoさん
- [ ] **2. `PrivacyPolicy-ja.md` の旧アプリ名5箇所を「つきねこ」へ**（1と同時に行う）
- [ ] **3. 「つきねこ」の商標確認**（J-PlatPat または弁理士）— harutoさん
- [ ] **4. App Store Connect の入力13項目**
      公開URL2つ／サポート用メール／スクリーンショット／掲載名・サブタイトル／
      概要・キーワード・カテゴリ／App Privacy回答／年齢区分／App Review連絡先／
      「サインインが必要です」をオフ／価格と配信地域／リリース方法／最終承認。
      **エージェントが代行してよいが、入力値の全文を提示して承認を得てから。
      「審査へ提出」ボタンは押さない**
- [ ] **5. App Store用スクリーンショット** — 撮影はharutoさん、加工はCodex。
      **リデザイン後の画面で、借入の画面を含めて撮り直す**
- [ ] **6. サポート公開URLとサポート用メールアドレスを決める** — harutoさん

### C. harutoさんの判断待ち

- [ ] 同梱サービスカタログ64件の過不足
- [ ] 支払い予告の範囲を3ヶ月とした妥当性、通知でも知らせるか
- [ ] 取り込み済みブランチ3本を削除するか
      （`design/black-cat-redesign` / `feat/onboarding-tutorial` / `feat/search-scroll-to-item`。
      **いずれも main に含まれている**。削除は不可逆だがコミットは残る）

### D. コード品質（急がない）

- [ ] `ReportCard.swift` が776行（規約の400行目安を超過。分割候補）
- [ ] `aps-environment` が無いのに `Info.plist` が `UIBackgroundModes: remote-notification` を
      宣言している。**CloudKit同期がプッシュで届かず、使っていない宣言は審査で指摘されうる**。
      capability追加はApp IDの更新を伴うため、提出直前に触るかは要判断
- [ ] `.verify-*` に古いビルド出力が945MB（gitignore済みで実害はない）

---

## 2026-08-09 に完了したこと

すべて **main にマージ済み**、ビルド12〜15としてTestFlightへ配布済み。

- [x] **ビルド12のアップロード**（8/8の失敗はAppleの定期メンテナンスが原因と確定。再送で成功）
- [x] **検索して見つけた項目まで到達できるようにした**
      `dismissSearch` が検索文字を消す仕様のため、**候補を選んでも絞り込まれていなかった**。
      `searchCompletion` へ差し替え。検索中は一覧より上を畳み、状態の絞り込みを外す
- [x] **ダークモードで猫が地に沈んで見えないのを直した**（コントラスト 1.04:1）。
      体は墨のまま、同じシルエットをぼかして背後へ敷く。回帰テストで数値を縛った
- [x] **チュートリアルに猫の状態の読み方を説明するページを追加**（全5ページへ）
- [x] **画面に残っていた4箇所目の旧アプリ名を修正**し、`AppInfo.displayName` で出どころを1つに縛った
- [x] **通知が届かない不具合を直した**（ローカル通知の64件上限を超えて黙って捨てられていた）。
      **実機で配信を確認済み**
- [x] **通知をタップするとクラッシュする不具合を直した**（`.ips` を記号化して原因を確定）。
      **実機で解消を確認済み**
- [x] **Codexへ渡すのは画像生成のみ**へ方針変更し、共通ルール・スキル・AGENTS.md・
      MEMORY・使用上限ガードの5箇所を揃えた
- [x] **審査前の全体レビュー**（掲載文言の機能主張と実装の一致を確認。ブロッカー5件を洗い出し）
- [x] **main へのマージ**（90コミット・161ファイル。fast-forward）

---

## 次に着手するときの順序

1. **3（商標）をharutoさんが確認し始める**
2. その間にエージェントが **1・2（プライバシーポリシーの文言修正と公開用HTMLの用意）**
3. harutoさんが **Pages の有効化** と **5（スクリーンショット撮影）**
4. **4（App Store Connect の入力）** をエージェントが承認を得ながら代行
5. **A の実機確認**を並行して消す（特に最優先の1件）
6. 「審査へ提出」は**harutoさんが押す**
