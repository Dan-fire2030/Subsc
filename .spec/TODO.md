# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（借金・ローンの返済管理／2026-08-02 確定）

> **前サイクル（レポートカードの可視化スタイル）は完了。** `.spec/archive/TODO-2026-08-02-chart-done.md` を参照。

---

## 前サイクルからの繰り越し

- [ ] App Store Connect へ掲載名・サブタイトル・概要・キーワードを入力
      **ローン機能を1.0.0に含める決定により、掲載文の作り直しが必要**

---

## 本サイクル：借金・ローンの返済管理

**ブランチ：`feat/loan-repayment`**（AGENTS.md の Git運用ルールに従い、main で直接作業しない）

### フェーズ1：計算の土台（TDDで進める・ビューから切り離す）

**`LoanScheduleCalculator` を純粋計算として作る。** ここが正しくないと以降すべてが崩れる。

- [x] **先にテストを書く**：元利均等の毎月返済額が、電卓で検算した既知の値と一致すること
- [x] **先にテストを書く**：利率0%でゼロ除算しないこと（`P / n` へ分岐する）
- [x] **先にテストを書く**：最終回で残高がちょうど0になること（端数を元金へ寄せる）
- [x] 元利均等の予定表生成を実装する
- [x] **先にテストを書く**：無利息（`P / n`）と、最終回の端数調整
- [x] 無利息の予定表生成を実装する
- [x] **先にテストを書く**：リボ（残高スライド定額）で帯に応じた定額が引かれること
- [x] **先にテストを書く**：**元金が増えない帯設定を不正として弾くこと**（残高が永久に減らない）
- [x] リボの予定表生成と入力検証を実装する
- [x] **先にテストを書く**：ボーナス返済で完済が早まり、上乗せが全額元金へ充当されること
- [x] ボーナス返済を実装する
- [x] **先にテストを書く**：変動金利の履歴に従って、適用日以降の率で計算されること
- [x] 利率履歴に対応する
- [x] **先にテストを書く**：滞納で完済予定日が1ヶ月ずれ、残高が減らず、総利息が増えること
- [x] 滞納を実装する
- [x] **先にテストを書く**：繰上返済（期間短縮型）で回数と総利息が減ること
- [x] 繰上返済の試算を実装する
- [x] **先にテストを書く**：「現在残高から」と「当初元本から」で、同じ状態を表す入力が同じ予定表を出すこと
- [x] 2つの登録方式を実装する
- [x] **日付は `Calendar(identifier: .gregorian)` と固定日付を使い、実行日に依存させない**

### フェーズ2：データモデル

**CloudKitミラーリングの制約を必ず守る**（既定値かOptional、一意制約なし、配列はCSV、enumはRaw）。

- [x] `Loan` を追加する（`final class` / 全プロパティに既定値かOptional）
- [x] `rateHistoryCSV` と `slidingTiersCSV` を computed property で出し入れする
      （`leadDaysCSV` と同じ流儀。**配列を直接保存しない**）
- [x] `LoanPayment` を追加する（`Loan` へのリレーション）
- [x] `RepaymentMethod` / `InterestType` / `LoanOrigin` / `LoanPaymentStatus` を
      `String` rawValue の enum で追加し、**表示名は型側の `var title: String` に置く**
- [x] **unknown rawValue でクラッシュせずフォールバックすること**をテストする
- [x] `CostType` に `.loan`（借入・ローン）を追加する
- [x] **`SubscApp.swift` の `ModelContainer` に `Loan` と `LoanPayment` を明示する**
      （リレーション経由の暗黙の引き込みに頼らない。既存の `AmountEntry` が同じ問題を抱えている）

### フェーズ3：返済実績と通知

- [x] 契約内容から返済予定表を自動生成し、`LoanPayment` として保存する
- [x] 日付が過ぎた回を返済済みとして扱う（何もしなければ予定どおり）
- [x] 返済日当日のローカル通知を予約する
      **識別子は `subsc-loan-<clientID>-<year><month>`。既存の `subsc-<clientID>-...` を壊さない**
- [x] 通知アクション「返済した」「滞納」を追加し、**アプリを開かずに記録できるようにする**
- [x] 滞納を選んだときに、その月を0にして以降を1ヶ月ずらす
- [x] 返済額をあとから編集できるようにする（繰上返済もこの編集で扱う）
- [x] **通知の再スケジュールで既存の更新日通知を巻き込まないこと**をテストする
      （`NotificationIdentifierTests` に同種のテストがある）

### フェーズ4：画面（無料の範囲）

- [x] 費目一覧に「借入・ローン」種別として並べる
      **費目と混ぜて「次の期日」順**（2026-08-02にharutoさんが決定）。
      並び順と絞り込みは `DashboardListBuilder` に閉じた
- [x] ローンの追加・編集画面（2つの登録方式、返済方式、金利種別、ボーナス、リボの帯）
      **追加は「＋」のメニューから**（費目 / 借入・ローン）
- [x] **入力検証**：元金が増えない帯設定、返済回数0、利率が負などを日本語で弾く
      保存前に**予定表を1本作ってみる**方式にした（個別ルールでは拾いきれないため）
- [x] ローン詳細（残高、次回返済日、完済予定日、進捗）
- [x] 返済予定表（各回の日付・返済額・元金・利息・残高）
- [x] 返済履歴（返済済・滞納の記録と編集）
      **記録の取り消し**も追加（通知の押し間違いで完済予定日がずれたままになるため）
- [x] **1ファイル400行を目安に分割する**（最大は `LoanFormView+Sections.swift` の約380行）

### フェーズ5：試算（Pro候補・最初から分離して実装する）

**1.0.0 では無料で提供するが、後から切り出せるようにファイルと型を分ける。**

- [x] 繰上返済の試算画面（「今◯円入れると何ヶ月早まり、利息がいくら減るか」）
      **今の残高と残り回数から**試算する（当初の条件だと早まる月数が過大になる）
- [x] 利率変更の影響試算
- [x] 借入シミュレーター（未登録で試せる。借入額・利率・期間から返済額を出す）
      **設定画面に置いた。** 契約の詳細に置くと、登録前の人が辿り着けないため
- [x] **課金判定を差し込む箇所を1点に集約しておく**（3ヶ月後の判断で足せるように）
      `LoanSimulationAccess.isUnlocked` の1つだけ。入口はすべて `LoanSimulationLink` を経由させ、
      入口ごとの書き漏らしが起きないようにした。ファイルは `Features/LoanSimulations/` に分離済み

### フェーズ6：レポートへの統合

- [ ] `ReportCalculator` が返済額（元金＋利息）を集計に含めること
- [ ] 種別での絞り込みに「借入・ローン」が出ること
- [ ] 4スタイルのグラフで色が付くこと（`ReportChartPalette` の追加のみで済むはず）
- [ ] **見込み扱いにしないこと**（予定表から確定的に決まる）
- [ ] 既存のレポート集計テストが壊れていないこと

### フェーズ7：CloudKit スキーマの反映（不可逆）

- [ ] Development署名のアプリで、**全プロパティに値を入れた**代表データを保存する
- [ ] CloudKit Console で `CD_Loan` / `CD_LoanPayment` のフィールドを1件ずつ突き合わせる
      **Optionalな `borrowedOn` `startedTrackingOn` `actualAmount` `recordedAt` `dueOn` は
      値を保存するまでフィールドが作られない。前回 `CD_endDate` でこの事故を起こしている**
- [ ] **Production へ Deploy**（不可逆）。**2026-08-02よりエージェントが代行する。**
      Claude in Chrome で Console を開き、**差分を一覧で提示して承認を得てから**押す

### フェーズ8：検証とリリース準備

- [ ] ビルドとユニットテスト全件
- [ ] **実機確認**（haruto さん。エージェントはシミュレーターを操作しない）
      通知アクションからの記録、滞納後の予定ずれ、予定表の表示
- [ ] プライバシーポリシー本文の更新（**借入情報が保存対象に加わる**。
      現行は最終更新2026-07-27で固定費すら反映されていない）
- [ ] App Store の掲載文・スクリーンショットの作り直し（`AppStore/Metadata-ja.md`）
- [ ] TestFlight ビルド9の配布（`/ios-testflight-release`）
- [ ] 審査提出の準備（`/ios-appstore-submit`）

---

## 収益化（リリース3ヶ月後に判断・2026-08-02決定）

- [ ] リリースから3ヶ月後、App Store Connect の継続率とダウンロード数を確認する
- [ ] 継続率が立っていれば、CSV/PDFエクスポートと年間レポートの詳細分析、
      **およびローンの試算3種**を買い切りのPro解放として有料化する（¥800〜1,200想定）
- [ ] ホーム画面ウィジェットは**無料**で実装する（起動頻度を上げるため）

---

## さらに別サイクルへ

- [ ] 停止中にすると停止前の実績まで過去レポートから消える（Codexレビュー Medium 6）
- [ ] `SettingsView` に変動費のリマインドを一括オン／オフする設定を置くか検討
- [ ] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する

---

## 完了済み

（このサイクルではまだなし。過去分は `.spec/archive/` を参照）
