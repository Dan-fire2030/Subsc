# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（固定費の記録／2026-07-31 確定）

## 優先度：高（このサイクルの本体）

### フェーズ1：データモデル
- [ ] `CostType` enum を追加（`String` rawValue、`title`、アイコン名）
- [ ] `PaymentMethod` enum を追加（`String` rawValue、`title`）
- [ ] `Subscription` に `costTypeRaw` / `hasVariableAmount` / `paymentMethodRaw` / `paymentMethodNote` を追加（すべて既定値つき）
- [ ] `AmountEntry` モデルを追加し、`Subscription` と双方向Optionalリレーションを張る
- [ ] `ModelContainer` のスキーマに `AmountEntry` を登録
- [ ] `project.pbxproj` へ新規ファイルを登録
- [ ] ビルドと既存テストが通ることを確認（既存データが壊れていないこと）

### フェーズ2：金額の解決とレポート
- [ ] 実績の検索・上書き保存を担う型を追加（同じ年月は1件に保つ）
- [ ] 「その月の金額」を解決するロジックを追加（実績 → 直近の実績 → 0円）
- [ ] `ReportCalculator` を変動費に対応させる
- [ ] **先にユニットテストを書く**：実績あり／未入力で直近を使う／実績ゼロ件／年集計での合算
- [ ] 見込みか実績かを呼び出し側へ返せるようにする（画面で印を出すため）

### フェーズ3：入力画面
- [ ] フォームに種別Pickerを追加
- [ ] フォームに「金額が毎月変わる」トグルを追加し、金額欄のラベルと保存先を切り替える
- [ ] フォームに支払い方法Pickerと補足メモを追加
- [ ] 詳細画面に種別・支払い方法を表示
- [ ] 詳細画面に「月別の記録」セクションと記録の入力導線を追加
- [ ] 見込みの月に印を表示

### フェーズ4：絞り込み
- [ ] 一覧の絞り込みに種別を追加
- [ ] レポートカードに種別の絞り込みを追加（既定は全種別の合計）

### フェーズ5：リマインド通知
- [ ] 月末リマインドの識別子体系を決め、更新日通知と分離する
- [ ] 未入力の変動費に対して月末リマインドを予約する
- [ ] 実績が入力されたらその月のリマインドを取り消す
- [ ] **ユニットテスト**：予約対象の判定、識別子が更新日通知と衝突しないこと

### フェーズ6：検証とリリース準備
- [ ] ビルドとユニットテスト全件
- [ ] シミュレーターで目視確認（登録・記録・レポート・絞り込み）
- [ ] Development署名で代表データを保存し、CloudKit Console で `AmountEntry` と新フィールドを確認
- [ ] Production へスキーマを反映
- [ ] `AppStore/RELEASE_RUNBOOK.md` を更新（ビルド番号・変更内容）
- [ ] ストア掲載文・キーワードを「固定費管理」向けに書き直す（案の作成まではエージェント、入力は haruto さん）

## 優先度：中
- [ ] `SettingsView` に、変動費のリマインドをまとめてオン／オフする設定を置くか検討

## 優先度：低
- [ ] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する

## 継続中の確認事項
- [ ] レポートの棒グラフと「サービス別料金」シートを、金額 > 0 のデータで目視確認する
- [ ] 絞り込みピッカーの切り替えと `DashboardEmptyState` の目視確認
- [ ] UIテストの実行確認（CLIからは実行できないためXcodeが必要）

## 完了済み
（このサイクルではまだなし。過去分は `.spec/archive/TODO-2026-07-31.md` を参照）
