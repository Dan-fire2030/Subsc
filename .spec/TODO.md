# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（起動時チュートリアル・2026-08-08 確定）
ブランチ：`feat/onboarding-tutorial`（ベースは `design/black-cat-redesign`）

---

## 前サイクルからの繰り越し（黒猫リデザイン）

- [ ] **設定画面のアイコン素材の差し替え**（`AppIconPreview.imageset/icon-1024.png`）
      **Codexへ委譲する**（使用上限の復帰は 2026-08-08 12:53）。
      原本は `.output/design-system/brand/app-icon-f.svg`。**ビルド11に旧アイコンが混入している**
- [ ] **TestFlight ビルド12**（上のアイコンを入れてから）
- [ ] **実機での確認**（harutoさんの作業）
      iOS 17〜25のフォールバック表示／アイコンの29pt表示／空状態の案内の猫／
      返済予定表・履歴／検索／金額入り通知の実文面
- [ ] **main へのマージ**（harutoさんの判断で**実機確認の後**。現在52コミット差）
- [ ] **「つきねこ」の商標確認**（J-PlatPat）
- [ ] `PrivacyPolicy-ja.md` のアプリ名差し替え（**公開済みURLの文書。タイミングを決める**）
- [ ] Codexによる adversarial review（レビューは自分のコードを自分で読んだため）
- [ ] 同梱サービスカタログ64件の過不足の判断（harutoさん）
- [ ] 支払い予告の範囲を3ヶ月とした妥当性、通知でも知らせるかの判断
- [ ] App Store Connect の掲載情報入力（掲載名は `つきねこ - サブスク・固定費・ローン管理` に決定済み）
- [ ] App Store用スクリーンショットの撮影（撮影はharutoさん、加工はCodex）

---

## フェーズ1：表示要否のストア（TDD）

**先にテストを書く。** 実行日に依存させない。

- [x] `OnboardingStore`（`UserDefaults` 注入・`@Observable`）
      既存の `ThemeStore` / `LoanNotificationSettings` と同じ流儀に揃える
- [x] テスト：初回は出す
- [x] テスト：完了すると次から出ない
- [x] テスト：スキップも完了と同じ扱い
- [x] テスト：見直しても「見た」記録は消えない
- [x] テスト：保存値が壊れていても起動を止めない

## フェーズ2：チュートリアル画面

- [x] 1ページぶんのビュー（見出し・本文・絵・ドット）
- [x] 4ページの内容を流し込む（費目の登録／借入と返済予定表／レポートの見方／通知とデータ）
- [x] スキップと「はじめる」
- [x] **1ページ目にだけ猫を「案内」の姿で座らせる**（`CatMood.guiding`）
- [x] 最大文字サイズでページ内をスクロールさせる（**固定高さで `clipped()` しない**）
- [x] `reduceMotion` でページ送りのアニメーションを外す

## フェーズ3：組み込み

- [x] `RootView` へ差し込む（初回のみ被せる）
- [x] `SettingsView` に「チュートリアルをもう一度見る」を足す
- [x] pbxprojへ新規ファイルを登録する（**次の空き番は 152。`+` を含むパスは引用符が要る**）

## フェーズ4：確認

- [x] ビルドとユニットテストが通る（**既存テストは落ちてはいけない**）
- [x] ライト・ダーク両方で確認
- [x] 最大文字サイズで確認
- [x] **CloudKitスキーマの変更が無いことを確認する**（`@Model` に触っていないこと）
- [ ] 独立レビュー（Codex復帰後に `/codex:rescue`）
- [ ] 実機での確認（harutoさん）。**最大文字サイズでの見え方は特に見てほしい**

**pbxprojの次の空き番は 156。** Onboardingグループは `DD0000230000000000000023`。
