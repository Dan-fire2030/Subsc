# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（テーマ色／2026-08-01 確定）

## 前サイクル（固定費の記録）からの繰り越し

**完了していないまま新サイクルへ移ったもの。** 詳細は `.spec/archive/TODO-2026-08-01.md` と
`ios/Subsc/AppStore/RELEASE_RUNBOOK.md` を参照。

- [ ] シミュレーターでの目視確認（登録・記録・レポート・絞り込み）※ haruto さんが実施中
- [ ] **CloudKit の Production へスキーマを反映**（不可逆・haruto さんの操作）
      Development 側は生成確認済み。`CD_AmountEntry` と `CD_Subscription` の5フィールド追加
- [ ] `feat/cost-type-data-model`（15コミット）を main へマージ
- [ ] ビルド5の Archive とアップロード、RELEASE_RUNBOOK への追記
- [ ] App Store Connect へ掲載名・サブタイトル・概要・キーワードを入力（haruto さんの作業）

## 別サイクルへ送ったもの（着手しない）

- [ ] 停止中にすると停止前の実績まで過去レポートから消える（Codexレビュー Medium 6）。
      「いつからいつまで利用中だったか」を保存するモデルが必要で、SPECにない追加要件のため
- [ ] `SettingsView` に、変動費のリマインドをまとめてオン／オフする設定を置くか検討
- [ ] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する

## 本サイクル：テーマ色

### フェーズ1：色のモデルと変換ロジック（TDDで進める）

- [ ] 既存の色定義を1箇所へ寄せる。`SubscriptionFormView.swift` の `colorPresets` と
      `SubscriptionFormView+CategoryColor.swift` の名前定義を共用できる型にまとめる
      （**既存の見た目と挙動を変えないこと**。純粋な移動にとどめる）
- [ ] **先にテストを書く**：HSB変換の往復、極端な入力（真っ白・真っ黒・彩度0）で破綻しないこと
- [ ] 16進文字列 ↔ 色 の変換に既存 `ColorHex` を流用できるか確認し、足りなければ拡張する
- [ ] **先にテストを書く**：カード色の補正（明度・彩度を白文字が読める範囲へ収める）の下限・上限
- [ ] 補正ロジックを実装する
- [ ] **先にテストを書く**：1色から3色グラデーションを生成する（色相を左右へずらす）
- [ ] グラデーション生成を実装する

### フェーズ2：設定の保存と読み出し

- [ ] `@AppStorage` でボタン色・カード色を保存する型を追加する（既定値は現在の色）
- [ ] **CloudKitのスキーマを変更していないことを確認する**（Console の Development に差分が出ない）
- [ ] 既定へ戻す操作を実装する

### フェーズ3：適用

- [ ] ボタン色を `App/RootView.swift:41` の `.tint(...)` へ適用する
- [ ] カード色を `ReportGlassStyle.swift` の `LiquidGlassCardBackground` へ適用する
- [ ] `glassEffect` のtintと影の色を連動させる
- [ ] 画面背景（`AppLiquidGlassBackground`）を連動させる
- [ ] 設定画面ヘッダー・空状態・`DashboardView`・`NotificationTimingView` を連動させる
- [ ] **状態色（利用中/停止中の緑・オレンジ、削除の赤）を変えていないこと**を確認する

### フェーズ4：設定画面

- [ ] 設定画面に「テーマ」セクションと2項目を追加する
- [ ] 色選択画面（プリセット一覧＋「その他の色」の `ColorPicker`）を作る
- [ ] 選択結果のプレビューを出す（カード色はレポートカードの縮小版）
- [ ] 「既定に戻す」を置く

### フェーズ5：検証

- [ ] ビルドとユニットテスト全件
- [ ] シミュレーターで目視確認（プリセット・自由選択・再起動後の保持・既定へ戻す）
- [ ] **iOS 17〜25 のフォールバック表示**も確認する（`glassEffect` が効かない系統）
- [ ] Codex による独立レビュー（設計判断を含むため `/codex:adversarial-review` も検討）
- [ ] iOS 26 実機での確認を haruto さんへ依頼する

## 完了済み

（このサイクルではまだなし。過去分は `.spec/archive/TODO-2026-08-01.md` を参照）
