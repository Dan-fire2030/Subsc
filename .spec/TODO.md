# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（ローンの一時停止／グラフのガラス表現・2026-08-03 確定）

> **前サイクル（借金・ローンの返済管理）は実装完了。** `.spec/archive/TODO-2026-08-02-loan.md` を参照。
> 未完了ぶんは下の「前サイクルからの繰り越し」へ引き継いだ。

---

## 前サイクルからの繰り越し

- [ ] **Codex による独立レビュー**（`git diff main...feat/loan-repayment` を読み取り専用で）
      **使用上限のため未実行。復帰は 2026-08-08 12:53。** 依頼の要点は
      `.agent/handoff/HANDOFF.md` の「レビュー依頼の要点」を再利用する
- [ ] **TestFlight ビルド10の配布**（ビルド9に未反映の修正が8件＋本サイクルの成果）
- [ ] `feat/loan-repayment` を main へマージ
- [ ] App Store Connect へ掲載名・サブタイトル・概要・キーワードを入力
      （掲載文は `AppStore/Metadata-ja.md` に作成済み。**入力はスクリーンショットと公開URLが揃ってから**）
- [ ] App Store用スクリーンショットの撮り直し（**借入の画面を含める**。実画面を haruto さんが撮り、Codex が加工）
- [ ] 審査提出の準備（`/ios-appstore-submit`）。**「審査へ提出」ボタンは押さない**

---

## 本サイクル A：ローンの一時停止

**ブランチ：`feat/loan-pause`**（ベースは `feat/loan-repayment`）

### A-1. モデル

- [ ] `Loan` へ `isPaused: Bool = false` と `pausedOn: Date?` を追加する
      （**既定値付きか Optional**。CloudKitミラーリングの制約）
- [ ] ビルドが通ることを確認する

### A-2. 繰り延べの計算（TDDで進める・ビューから切り離す）

**滞納（`missedPeriods`）と同じ仕組みで実装しない。** 利息を発生させない点が決定的に違う。

- [ ] **先にテストを書く**：停止→3ヶ月後に再開で、残りの `scheduled` の `dueOn` が
      ちょうど3ヶ月後ろへずれること
- [ ] **先にテストを書く**：そのとき**総利息と残高が変わらない**こと（滞納との違いを固定する）
- [ ] **先にテストを書く**：同じ月のうちに再開したら**何もずれない**こと（満月数が0）
- [ ] **先にテストを書く**：繰り延べ後の `dueOn` が `paymentDay` へ丸め直されること
      （31日指定で2月を跨ぐ場合。**前サイクルで踏んだ月末ずれの再発防止**）
- [ ] 繰り延べを実装する（`LoanPaymentStore` へ `pause` / `resume` を足す）
- [ ] **日付は `Calendar(identifier: .gregorian)` と固定日付**を使い、実行日に依存させない

### A-3. 予定表の自動進行

- [ ] **先にテストを書く**：停止中は `settlePastDue` が回を進めないこと
- [ ] `LoanPaymentStore.settlePastDue` に停止中の分岐を入れる
- [ ] 再開時は `dueOn` を書き換えた**後で** `settlePastDue` を走らせる（順序を逆にしない）

### A-4. レポート

- [ ] **先にテストを書く**：停止中のローンが**現在の期間**の集計から外れること
- [ ] **先にテストを書く**：停止中のローンが**過去の期間**の集計には残ること
      （**前サイクルで実際に踏んだ不具合。同じ穴を繰り返さない**）
- [ ] `ReportCalculator` のローン集計に停止の分岐を入れる（既存の期間ベースの規則に合わせる）

### A-5. 通知

- [ ] **先にテストを書く**：停止中のローンに返済日通知が予約されないこと
- [ ] **先にテストを書く**：再開で予約し直されること
- [ ] `LoanNotificationPlanner` に停止の分岐を入れる
- [ ] 停止操作でそのローンの予約を取り消す
- [ ] **名前空間 `subsc-loan-` を変えない。** 取り消しは自分の名前空間の中だけで行う

### A-6. 画面

- [ ] `LoanDetailView` に「一時停止する」／「返済を再開する」ボタンを置く
- [ ] 停止中は、いつから停止しているかと**完済予定が後ろへずれること**を伝える
- [ ] **先にテストを書く**：完済済み（`isClosed`）のローンは停止できないこと
- [ ] 完済済みのときは停止の入口を出さない（詳細画面・スワイプの両方）
- [ ] `DashboardView.loanRow` の **leading** に「停止」／「再開」のスワイプを足す
      （`allowsFullSwipe: false`、`pause.fill`＋オレンジ／`play.fill`＋緑。**サブスク行と揃える**）
- [ ] `LoanRow` を停止中は薄くし、「停止中」バッジを出す（サブスクの停止中と同じ値に揃える）
- [ ] **編集フォームにはトグルを置かない**（保存を押すまで反映されず挙動が食い違うため）

### A-7. 検証

- [ ] `xcodebuild build` が通る
- [ ] `xcodebuild test -only-testing:SubscTests` が全件通る
- [ ] コミットする（`<type>: <日本語の説明>` 形式）

### A-8. CloudKit Production 反映（**不可逆**）

- [ ] Development 署名のアプリで**実際に停止操作をして**代表データを保存する
      （`pausedOn` は Optional なので、値が入るまでフィールドが作られない）
- [ ] CloudKit Console で Development と Production の**差分を一覧にする**
- [ ] 反映される Record Type とフィールドを提示し、**不可逆であることを明示して承認を得る**
      （`AskUserQuestion`）
- [ ] Deploy する（**1回目が `XHR request failure` で落ちても再試行する**）
- [ ] 反映後、Production 側のフィールドを**1件ずつ読んでモデルと突き合わせる**
- [ ] `AppStore/RELEASE_RUNBOOK.md` の「ローン機能で追加したスキーマ」を更新する（独立したコミット）

---

## 本サイクル B：レポートグラフのガラス表現

**ブランチ：`feat/glass-charts`**（ベースは `feat/loan-repayment`。Aとは独立）

### B-1. 共通化

- [ ] `ReportGlassStyle.swift` へ、グラフ要素用のガラス表現を追加する
      （iOS 26 は `glassEffect` ＋ティント、iOS 17〜25 は半透明の塗り＋光沢＋リムライト）
- [ ] **4ファイルへ個別に書かない。** 後から質感を揃え直せなくなる

### B-2. 適用

- [ ] `BubbleChart`（円）へ適用する
- [ ] `BubbleCallout`（吹き出し）へ適用する
- [ ] `GlassBarChart`（縦バー）へ適用する
- [ ] `StackedBarChart`（積み上げ1本帯）へ適用する
- [ ] `ConcentricRingChart`（同心リング）へ適用する
- [ ] **見込み（`isEstimated`）の半透明表現が潰れていない**ことを目視で確認する
- [ ] 白いラベルの可読性が落ちていないことを目視で確認する

### B-3. 浮遊のアニメーション

- [ ] 上下と左右を**別々の周期**で往復させ、8の字に近い軌跡にする
- [ ] 円ごとに周期と位相をずらす（既存の `driftIndex` の考え方を踏襲）
- [ ] 振れ幅は**円同士が重なって見えない範囲**に抑える（半径に対する比で決める）
- [ ] **`reduceMotion` では一切動かさない**（既存の分岐を維持）
- [ ] 脈動（拡大率の伸縮）は**入れない**

### B-4. 壊していないことの確認

- [ ] **`blocksPaging` の `onChange` に `initial:` を付けていない**
- [ ] バブルのズーム／パン／ダブルタップのリセットが従来どおり動く
- [ ] 小さい円のタップ判定（`handleTap` の座標変換）がずれていない
- [ ] カード本体の慣性スライドと、縦バーの横スクロールが従来どおり動く

### B-5. 検証

- [ ] `xcodebuild build` が通る
- [ ] `xcodebuild test -only-testing:SubscTests` が全件通る
- [ ] コミットする

---

## 仕上げ

- [ ] 2本のブランチを `feat/loan-repayment` へマージする
      （**マージ前に `git log --oneline` と `git diff --stat` で範囲を確認する**）
- [ ] KNOWLEDGE.md に学びを記録する
- [ ] ビルド10としてまとめて TestFlight へ配布する

---

## haruto さんにお願いすること

- [ ] ガラス表現の見え方（**実機**。開発機には iOS 26 のランタイムしかない）
- [ ] 浮遊の速さと振れ幅が「ふわふわ」に見えるか、うるさくないか
- [ ] 停止中バッジの見え方と、詳細画面の文言
- [ ] 一覧スワイプの操作感（誤操作しないか）
- [ ] Codex のクレジット購入（レビューを 8/8 より前倒ししたい場合）
- [ ] GitHub Pages の有効化（プライバシーポリシー／サポートページの公開URL）

---

## さらに別サイクルへ

- [ ] `SettingsView` に変動費のリマインドを一括オン／オフする設定を置くか検討
- [ ] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する
- [ ] iOS 17〜25 のフォールバック表示の確認（開発機にランタイムが無い。従来からの積み残し）

---

## 収益化（リリース3ヶ月後に判断・2026-08-02決定）

- [ ] リリースから3ヶ月後、App Store Connect の継続率とダウンロード数を確認する
- [ ] 継続率が立っていれば、CSV/PDFエクスポートと年間レポートの詳細分析、
      **およびローンの試算3種**を買い切りのPro解放として有料化する（¥800〜1,200想定）
- [ ] ホーム画面ウィジェットは**無料**で実装する（起動頻度を上げるため）

---

## 完了済み

（このサイクルではまだなし。過去分は `.spec/archive/` を参照）
