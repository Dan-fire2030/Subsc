# TODO - タスクリスト

SPEC：`.spec/SPEC.md`（レポートカードの可視化スタイル／2026-08-01 確定）

## 前サイクルからの繰り越し

**完了していないまま新サイクルへ移ったもの。**

### 固定費の記録（`feat/cost-type-data-model`・15コミット）

- [ ] **CloudKit の Production へスキーマを反映**（不可逆・haruto さんの操作）
      Development 側は生成確認済み。`CD_AmountEntry` と `CD_Subscription` の5フィールド追加
- [ ] main へマージ
- [ ] ビルド5の Archive とアップロード、`RELEASE_RUNBOOK.md` への追記
- [ ] App Store Connect へ掲載名・サブタイトル・概要・キーワードを入力

### テーマ色（`feat/theme-color`・8コミット）

- [ ] **iOS 17〜25 のフォールバック表示の確認**（`glassEffect` が効かない系統）
      このMacには iOS 26 のランタイムしか無い。**リリース前には必ず潰すこと**
- [ ] iOS 26 実機での確認（**追加機能を入れてからまとめて実施**と決定）
- [ ] 見送ったレビュー指摘（詳細は `.spec/archive/TODO-2026-08-01-chart.md`）
      iOS 26のGlass透過／ボタン色のコントラスト／補正の説明文言／Dynamic Type／
      無反応のサンプルボタン／デバウンス／プリセット共用

### さらに別サイクルへ

- [ ] 停止中にすると停止前の実績まで過去レポートから消える（Codexレビュー Medium 6）
- [ ] `SettingsView` に変動費のリマインドを一括オン／オフする設定を置くか検討
- [ ] ルート `README.md` の Web版セクションは、Web版の廃止が進んだら削除する
- [ ] `SubscApp.swift` の通常時の `ModelContainer` に `AmountEntry` が明示されていない
      （リレーション経由で引き込まれるため現状は動くが、外した瞬間に静かに壊れる）

## 本サイクル：レポートカードの可視化スタイル

### フェーズ1：データの土台（TDDで進める）

- [x] `CostType` に色を持たせる（`title` / `systemImage` と同じ流儀で型側に置く）
- [x] **先にテストを書く**：`ReportEntry` に `costType` を持たせ、`ReportCalculator` が詰めること
- [x] `ReportEntry` へ `costType` を追加する
- [x] **先にテストを書く**：種別ごとの集計（金額の合計、見込みが1件でもあれば見込み扱い、
      0円の種別を出さない、0件・1件、全件同額）
- [x] 種別ごとの集計を実装する
- [x] **先にテストを書く**：バブルの配置（面積が金額に比例する、重ならない、
      初期倍率で全件が収まる）
- [x] バブルの配置計算を実装する（ビューから切り離した純粋な計算にする）

### フェーズ2：スタイルの選択と保存

- [ ] `ReportChartStyle` enum（`bar` / `ring` / `bubble` / `column`）を追加する
- [ ] `ThemeStore` へ保存する（既定は `bar`。`resetToDefaults` の対象に含める）
- [ ] **CloudKitのスキーマを変更していないことを確認する**

### フェーズ3：4つのスタイルの実装

- [ ] 共通：色の決め方（絞り込みの有無で種別色／費目色を切り替える）を1箇所に寄せる
- [ ] 共通：見込みを半透明で描き、VoiceOverで「見込み」と読ませる
- [ ] A. 積み上げ1本帯
- [ ] B. 同心リング
- [ ] C. バブル（ピンチズーム、ダブルタップで初期倍率、**等倍ではパンしない**）
- [ ] D. 縦バー（横スクロール・慣性、端のフェード、収まるときはスクロール無効）
- [ ] どのスタイルでもタップで `ReportBreakdownSheet` を開けること

### フェーズ4：設定画面

- [ ] 「テーマ」セクションへ「グラフの表示」を追加する
- [ ] 選択画面（4つ＋プレビュー）を作る
- [ ] 「既定に戻す」でスタイルも戻ること

### フェーズ5：検証

- [ ] ビルドとユニットテスト全件
- [ ] **カードの慣性スライドが壊れていないこと**を目視で確認する
- [ ] **Cが等倍でページ送りでき、ズーム中はパンすること**を目視で確認する
- [ ] **Dの横スクロール中にページが変わらないこと**を目視で確認する
- [ ] 費目0件・1件・全件同額・1件だけ極端に大きい場合の見た目
- [ ] アクセシビリティ文字サイズ（グラフ領域340pt）での見た目
- [ ] Codex による独立レビュー
- [ ] iOS 26 実機での確認を haruto さんへ依頼する

## 完了済み

（このサイクルではまだなし。過去分は `.spec/archive/` を参照）
