# TODO - タスクリスト

最終整理：2026-08-11
ブランチ：**`main`**（ビルド16をTestFlightへ投入済み）
現在のビルド：**20**（識別情報の唯一の情報源は `ios/Subsc/AppStore/RELEASE_RUNBOOK.md`）

前サイクル（カレンダー画面／ビルド16の投入／公開ページの用意）の一覧は
`.spec/archive/TODO-2026-08-11.md` にある。

---

## 進行中：アーカイブ機能（2026-08-11・`feat/archive`）

SPEC：`.spec/SPEC.md`（2026-08-11 確定）

**【最重要】`@Model` にフィールドを足すため、CloudKit の Production 反映が要る。反映は不可逆。**
pbxprojへの登録は手作業（**次の空き番は 176**。グループUIDは `DD` の 25 から）。

### フェーズ1：判断をテストできる型へ閉じる（TDD）

**先にテストを書く。** `now` と `calendar` は既定値付き引数で注入する。

- [ ] `ArchivePolicy`（アーカイブ中か／残り日数／期限切れか）
- [ ] テスト：`archivedAt == nil` はアーカイブしていない
- [ ] テスト：残り日数が**日付の境目**で切り替わる（時刻で比べない）
- [ ] テスト：**30日ちょうどはまだ残る／31日目で消える**
- [ ] テスト：うるう年・月またぎ

### フェーズ2：モデルと除外（TDD）

- [ ] `Subscription.archivedAt: Date?` を追加（**Optional。既定値なしにしない**）
- [ ] `Loan.archivedAt: Date?` を追加
- [ ] `DashboardListBuilder` の入口でアーカイブ中を外す
- [ ] `ReportCalculator.includes` でアーカイブ中を外す
- [ ] テスト：アーカイブ中が**一覧・レポート・カレンダーのすべてから消える**
- [ ] テスト：**アーカイブしても借入の返済予定表が消えない**
- [ ] テスト：復元で元の状態（利用中／停止中）へ戻る

### フェーズ3：操作と画面

- [ ] 詳細画面に「アーカイブ」を足す（**削除は残す**）
- [ ] 一覧の右スワイプに「アーカイブ」を足す（削除と並べる）
- [ ] アーカイブ時に**予約済み通知を取り消す**／復元時に組み直す
- [ ] 絞り込みに「アーカイブ」を追加（段が5つになる）
- [ ] 行ごとの**残り日数**（**残り7日以下は色を変える**）
- [ ] 行ごとの「復元」と「削除」（削除は吹き出しの確認）
- [ ] 「すべて削除」（**件数付きの確認**。空のときは出さない）
- [ ] 「復元」で戻せる短い知らせ（アーカイブ直後）

### フェーズ4：自動削除

- [ ] 起動時とアーカイブ欄を開いたときの両方で期限切れを消す
- [ ] 削除時に**予約済み通知も取り消す**
- [ ] テスト：期限切れの抽出

### フェーズ5：確認と反映

- [ ] ビルドとユニットテストが通る（**既存527件は1件も落ちてはいけない**）
- [x] **シミュレーターで実際にアーカイブして保存し、Console にフィールドが出ることを確認**
- [x] **モデルの保存プロパティと Console のフィールドを1件ずつ突き合わせた**（26件・24件とも一致）
- [x] **Production へ反映済み**（2026-08-11・`Changes Deployed` と Production 側の再読み込みで確認）
- [x] **harutoさんが実機で確認**（2026-08-12・問題なし）
- [x] `main` へマージ（2026-08-11・fast-forward）
- [x] **ビルド18としてTestFlightへ**（2026-08-11・Apple側の受領に成功）
- [x] **ビルド19としてTestFlightへ**（2026-08-11）。ビルド18の見た目の不具合2件を修正
      （一括削除の確認が画面外へはみ出す／種別と並び替えが1つのボタンに同居）
- [x] **harutoさんが実機で確認**（2026-08-12・3項目すべて問題なし）。
      **`CD_archivedAt` 反映後の他の端末との同期も、ここで裏が取れた**

---

## 完了：カレンダーと一覧の使い勝手（2026-08-11・ビルド17／`main` へマージ済み）

harutoさんが実機で確認済み。詳細は `.spec/archive/SPEC-2026-08-11-usability.md` と
`AppStore/RELEASE_RUNBOOK.md` のビルド17の節。

- [x] カレンダーの年月をホイールで選べるようにした
- [x] 費目一覧を返済／月払い／年払いに分け、件数と合計を出した
- [x] 並び替え（期日・金額・名前／昇降）を追加。既定は金額の大きい順
- [x] 削除の確認を、押したボタンを元にした吹き出しへ変えた
- [x] 内訳シートの配色をアプリのパレットへ揃えた
- [x] **停止中の借入がある状態での起動時のCPUと発熱**（ビルド12以来の懸案。問題なし）

---

## 中断中：審査提出（アーカイブ機能の完了後に再開）

**スクリーンショットは完成済み。残るはASCへの入力と提出。**

**手順と担当区分の正本は `ios/Subsc/AppStore/RELEASE_RUNBOOK.md`。**

### 済んだもの

- [x] **商標確認**（2026-08-11・「つきねこ」で確定）
- [x] **プライバシーポリシーとサポートページの公開**
      <https://dan-fire2030.github.io/Subsc/privacy.html> / <https://dan-fire2030.github.io/Subsc/support.html>
- [x] **サポート用メールアドレス**（`haruto_1224@icloud.com`）
- [x] 掲載文言（`Metadata-ja.md`。カレンダーぶんも反映済み）

### 残っているもの

- [x] **スクリーンショット6枚**（2026-08-11 完成・harutoさん承認済み）。
      `.output/appstore/screenshots/` に配置。詳細は同ディレクトリの `README.md`
      - 実機の実画面に見出しを載せた形。**描き起こした画像は使っていない**
      - **1206×2622・PNG・アルファなし**をエージェントが `sips` で実測して確認
      - **6.9インチ枠用に 1320×2868 へ拡大した6枚を `6.9inch/` に追加**（2026-08-11）
      - 元画像は `source/` に保存（**実機とその時のデータが無いと撮り直せないため**）

- [x] **App Store Connect の入力**（2026-08-11・17項目を入力し、画面を読み取って保存を確認済み）

- [x] **【提出前の必須】6.9インチ枠へ6枚をアップロード**（2026-08-11・harutoさんがドラッグ）
      - 6.3インチ枠（1206×2622）だけでは提出できなかった。**6.9インチか6.5インチのどちらかが必須**。
        詳細は `.output/appstore/screenshots/README.md` の「6.9インチ枠の6枚が必須」
      - **アップロード直後は並びが `06,04,01,05,02,03` に崩れていた**。枠内のドラッグで正順へ直し、
        再読み込みで保存を確認した
      - 6.5インチ枠は自動で「6.9インチディスプレイを使用」に切り替わり6枚を継承
      - 6.3インチ枠の6枚は無傷（6/10・正順）
- [x] **著作権**（2026-08-11・`2026 Haruto Takamori`）。
      **必須だった。**「空でも保存できる」ことを根拠に任意と判断していたのは誤り
- [x] **コンテンツ配信権**（2026-08-11・「いいえ」）。アプリ情報側にあり、
      バージョンのページからは見えない。同梱カタログはサービス名のテキストと色だけで、
      **ロゴ画像を一切持たない**（`ServiceCatalog.swift` に明記）
- [x] **「審査用に追加」→「審査へ提出」**（2026-08-11 23:29・harutoさんが実行）。
      状態は **「1.0 審査待ち」**。**リリース方法は手動**なので、承認後も
      harutoさんが「リリース」を押すまで公開されない

### 提出後に残っているもの

- [x] 審査結果の受領（2026-08-18）。**却下**。Guideline 2.1(a) Performance - App Completeness。
      指摘は「通知を許可しないと費目を保存できない」。審査端末は iPad Air 11-inch (M3) / iPadOS 26.6
- [x] 原因の特定と修正（`5bc9fe3`）。通知の許可に関わらず保存を続けるようにした。
      届かないことは通知セクションの説明で伝える
- [x] 修正の動作確認（2026-08-18・harutoさん。通知を許可しない状態で保存できることを確認）
- [ ] ビルド20のアップロードとApp Store Connectでの差し替え
- [ ] **Appleへの返信**（何が原因でどう直したか）— 文面はharutoさんの承認を得てから入力する
- [ ] **「審査用に追加」→「審査へ提出」** — harutoさんが押す
- [ ] 承認後の**手動リリース** — harutoさんが押す

---

## 残タスク

### A. 実機で未確認（harutoさんの作業）

- [x] **停止中の借入がある状態での起動時のCPUと発熱**（2026-08-11・ビルド17で確認・問題なし）。
      **ビルド12で直した無限ループの実機確認が、ようやく取れた**
- [ ] **iOS 17〜25のフォールバック**（ずっと未検証。iOS 26以外の端末が要る）
- [ ] アプリを**完全終了した状態**での、返済日通知の「返済した」ボタン
- [ ] アイコンの実寸表示（29pt、明るい／暗い壁紙）
- [ ] 停止からの再開（`resume`）の画面動作

### B. harutoさんの判断待ち

- [ ] 同梱サービスカタログ64件の過不足
- [ ] 支払い予告の範囲を3ヶ月とした妥当性、通知でも知らせるか
- [ ] 取り込み済みブランチの削除（**いずれも main に含まれている**）

### C. コード品質（急がない）

- [x] **`ios/Subsc/.verify-*` と `.audit-*` の古いビルド出力（12件・約1.4GB）を削除**（2026-08-12）。
      7/26〜7/28の `xcodebuild` 出力で、gitignore済み・追跡ファイル0件。`xcodebuild` で再生成できる。
      `~/Library/Developer/Xcode/Archives/` のbuild16〜19には触れていない
