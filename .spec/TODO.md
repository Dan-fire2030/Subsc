# TODO - タスクリスト

最終整理：2026-08-11
ブランチ：**`main`**（ビルド16をTestFlightへ投入済み）
現在のビルド：**16**（識別情報の唯一の情報源は `ios/Subsc/AppStore/RELEASE_RUNBOOK.md`）

前サイクル（カレンダー画面／ビルド16の投入／公開ページの用意）の一覧は
`.spec/archive/TODO-2026-08-11.md` にある。

---

## 進行中：カレンダーと一覧の使い勝手（2026-08-11・`feat/list-and-calendar-usability`）

SPEC：`.spec/SPEC.md`（2026-08-11 確定）

**`@Model` に触らない＝CloudKit反映は不要。** 新規ファイルを作る場合、
pbxprojへの登録は手作業（**次の空き番は 176**。175まで使用済み。グループUIDは `DD` の 25 から）。

### フェーズ1：一覧の並び替えと分類（TDD）

**先にテストを書く。** `DashboardListItem.swift` の `DashboardListBuilder` に閉じる。

- [x] `DashboardSortOrder`（期日順 / 金額順 / 名前順、昇降の別）
- [x] テスト：3つの並び順が効く
- [x] テスト：**同額・同日でも順序が安定する**（最後は名前で決める）
- [x] テスト：名前順が日本語の並びになる（`localizedStandardCompare`）
- [x] `DashboardListSection`（返済 / 月払い / 年払い）と、その組み立て
- [x] テスト：借入が「返済」へ入る
- [x] テスト：セクションの件数と合計
- [x] テスト：**セクション合計が、その下に並ぶ行の金額の合計と一致する**
      （当初は `ReportCalculator` を通す予定だったが成立しないため変更。理由は SPEC）
- [x] テスト：検索中は分類しない
- [x] `DashboardSortStore`（選んだ並びを覚える。`UserDefaults` 注入・`@Observable`）

### フェーズ2：カレンダーの年月選択（TDD）

- [x] 年の範囲を組み立てる関数（登録データに追従。前後1年の余裕）
- [x] テスト：データが空 / 過去だけ / 未来だけ / 今年をまたぐ
- [x] 見出しをタップできるようにし、山形を添える
- [x] 年と月の2列ホイール（**iOS 26 は Liquid Glass、17〜25 はフォールバック**）
- [x] 選択日の丸め（既存の `clampedSelection` を通す）
- [ ] 矢印ボタンと横スワイプが従来どおり動くことを確認（**目視待ち**）

### フェーズ3：削除の確認を吹き出しにする

**ビューの見た目なのでユニットテストでは縛れない。シミュレーターで目視する。**

- [x] `DashboardView` の一覧からの削除（スワイプ・行を起点に）
- [x] `SubscriptionDetailView` の削除ボタン
- [x] `LoanDetailView` の削除ボタン
- [x] ~~`LoanHistoryView` / `LoanPaymentEditorView` の返済記録の削除~~ → **数え違いだった**。
      あれらは「操作を完了できませんでした」という別のアラートで、削除の確認ではない。
      **`LoanPaymentEditorView` の「記録を取り消す」にはそもそも確認が無い**（押すと即実行）。
      **確認を足さない判断**（2026-08-11・harutoさん。元の予定へ戻すだけでデータは消えないため）
- [x] **`.presentationCompactAdaptation(.popover)` を付ける**（無いとシートへ化ける）
- [x] 削除以外の確認に触っていないことを確認

### フェーズ4：内訳シートの配色

- [x] `ReportBreakdownSheet` の色を `BlackCatPalette.harmonized` 経由にする
- [x] グラデーションの作り（色 → 55%）を変えていないことを確認
- [x] テスト：保存値を渡したときの色がグラフ本体と一致する

### フェーズ5：確認

**テスト527件通過（既存503＋新規24）／Releaseビルドの警告ゼロ／`@Model` 未変更＝CloudKit反映は不要。**
コミットは `f2d16ef`。**`main` へは未マージ。**

- [x] ビルドとユニットテストが通る（**既存503件は1件も落ちてはいけない**）
- [x] **`@Model` に触っていないことを確認**
- [ ] **harutoさんが動かして確認**（2026-08-11に担当を決定）。確認項目は下記
- [ ] `main` へマージ
- [ ] ビルド17としてTestFlightへ（承認を得てから）

---

## 中断中：審査提出（スクリーンショットの途中で中断）

**手順と担当区分の正本は `ios/Subsc/AppStore/RELEASE_RUNBOOK.md`。**

### 済んだもの

- [x] **商標確認**（2026-08-11・「つきねこ」で確定）
- [x] **プライバシーポリシーとサポートページの公開**
      <https://dan-fire2030.github.io/Subsc/privacy.html> / <https://dan-fire2030.github.io/Subsc/support.html>
- [x] **サポート用メールアドレス**（`haruto_1224@icloud.com`）
- [x] 掲載文言（`Metadata-ja.md`。カレンダーぶんも反映済み）

### 残っているもの

- [ ] **スクリーンショット**。10枚撮って**5枚を採用と判定済み**（判定は下記）。
      **ホームの1枚だけ撮り直しが必要**（費目名に個人情報が入っている）
      - 採用：ホーム（要撮り直し）／カレンダー／返済予定表／サービスから選ぶ／チュートリアルの猫
      - **除外：費目一覧（メールアドレスが写り込み＋スクロール中でブレている）**
      - 見送り：グラフの表示・費目を追加・借入を追加（空フォーム）・ボタンの色
      - **今回の5件を直すと画面が変わるので、撮り直しは修正後にまとめて行う**
- [ ] 画素数の実測（1179×2556・アルファ無し）— ファイルを受け取ってから
- [ ] **App Store Connect の入力10項目**（承認を得ながらエージェントが代行）
- [ ] **「審査へ提出」** — harutoさんが押す

---

## 残タスク

### A. 実機で未確認（harutoさんの作業）

- [ ] **停止中の借入がある状態での起動時のCPUと発熱** ← **最優先**
      ビルド12で直した無限ループそのもの。**実機は一度も確認していない**
- [ ] カレンダーのライト・ダーク、**最大文字サイズ**、**iOS 17〜25のフォールバック**
- [ ] アプリを**完全終了した状態**での、返済日通知の「返済した」ボタン
- [ ] アイコンの実寸表示（29pt、明るい／暗い壁紙）
- [ ] 停止からの再開（`resume`）の画面動作

### B. harutoさんの判断待ち

- [ ] 同梱サービスカタログ64件の過不足
- [ ] 支払い予告の範囲を3ヶ月とした妥当性、通知でも知らせるか
- [ ] 取り込み済みブランチの削除（**いずれも main に含まれている**）

### C. コード品質（急がない）

- [ ] **`ios/Subsc/.verify-*` と `.audit-*` に古いビルド出力が合計1.4GB**（gitignore済みで実害はない）。
      **リポジトリ直下ではなく `ios/Subsc/` 配下にある**
